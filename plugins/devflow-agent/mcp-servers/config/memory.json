{
  "name": "memory",
  "description": "[DEPRECATED] Persistent knowledge graph - prefer native subagent memory instead",
  "deprecated": true,
  "deprecation_reason": "Native Claude Code subagent memory (memory: project) provides better integration. See agents/*.md for examples.",
  "command": "npx",
  "args": ["@anthropic-ai/mcp-server-memory"],
  "env": {
    "MEMORY_FILE": "${PROJECT_ROOT}/.devflow/memory.json"
  },
  "tools": [
    "create_entities",
    "create_relations",
    "add_observations",
    "delete_entities",
    "delete_relations",
    "delete_observations",
    "read_graph",
    "search_nodes",
    "open_nodes"
  ],
  "notes": {
    "persistence": "Stores memory graph in .devflow/memory.json",
    "usage": "Maintains context across autonomous iterations",
    "migration": "Use 'memory: project' in subagent frontmatter instead. Memory persists in .claude/agent-memory/<agent-name>/MEMORY.md"
  }
}
