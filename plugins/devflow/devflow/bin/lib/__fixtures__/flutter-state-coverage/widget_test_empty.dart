// Fixture: Empty widget test stub — no state coverage at all
// Used to verify that verifyCoverage returns status:'missing' with all declared states in blockers
// when the test file contains no state-management patterns.

import 'package:flutter_test/flutter_test.dart';

void main() {
  // no real test body
}
