---
objective: 34-ui-oracle-loop-w1b-surface-spec
trd: "04"
type: tdd
wave: 4
depends_on: ["34-03"]
files_modified:
  - plugins/devflow/devflow/bin/lib/ui-spec-validate.cjs
  - plugins/devflow/devflow/bin/lib/ui-spec-validate.test.cjs
  - plugins/devflow/devflow/bin/lib/ui-spec-cli.cjs
  - plugins/devflow/devflow/bin/lib/ui-spec-cli.test.cjs
  - plugins/devflow/devflow/bin/df-tools.cjs
  - plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/state-without-seed.md
  - plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/outage-equals-empty.md
  - plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/unknown-pattern.md
  - plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/hit-rect-overlap.md
  - plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/flow-ends-mid-route.md
  - plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/guard-without-denied-state.md
autonomous: false
fixture_strategy: generators
plan_row: "1b-02 (invariants I4-I8 + CLI arm) — docs/IMPLEMENTATION-PLAN-ui-oracle-loop.md Part 3, W1b table"
requirements:
  - "SPEC-34-09: invariants I4-I8 (states/seeds + outage≠empty; patterns + inherited must_not; hit_rect; flows; guards) each fail with their own code"
  - "SPEC-34-10: `df-tools ui spec validate <file>` exits 1 with the codes on stdout, exits 0 on the positive control, and stamps engine_version/schema_version"
  - "SPEC-34-11: the hit_rect static invariant (I6) has no statically-evaluable input in proposal §4.2 — it is resolved by a recorded decision, not by a guess"
must_haves:
  truths:
    - "`state-without-seed.md` fails with exactly `STATE001`."
    - "`outage-equals-empty.md` — a spec where `outage.content.must_show` equals `empty.content.must_show` — fails with exactly `STATE002` (§4.4's explicit schema rejection)."
    - "A spec missing one of §4.4's minimum states (populated, empty, error, outage, long-content, narrow, dark) fails `STATE003`, naming each missing state."
    - "`unknown-pattern.md` fails with exactly `PAT001`; a control of a pattern kind that drops one of that pattern's `must_not` defaults fails `PAT002`."
    - "`flow-ends-mid-route.md` — a flow whose last step is neither a `back` nor a declared terminal route — fails with exactly `FLOW002`; a flow step naming a control or route that does not exist fails `FLOW001`."
    - "`guard-without-denied-state.md` fails with exactly `GUARD001`, naming the guard and the route."
    - "`hit-rect-overlap.md` fails with exactly `HIT001` under whichever I6 model the checkpoint records — and the fixture's `<!-- BROKEN: -->` comment cites that decision."
    - "`df-tools ui spec validate __fixtures__/ui-spec/projects-rail.md` exits 0 and prints JSON carrying `ok: true`, `engine_version`, `schema_version`."
    - "`df-tools ui spec validate <any broken fixture>` exits 1 and prints the code list. Exit 1 is the gate contract from the plan's W1b table."
    - "All eleven broken fixtures still fail with exactly one code each — 34-03's I3h hygiene loop stays green with six new members."
  artifacts:
    - path: plugins/devflow/devflow/bin/lib/ui-spec-validate.cjs
      provides: "invariants I4-I8 added to the I1-I3 core; the complete §4.5 static invariant set"
    - path: plugins/devflow/devflow/bin/lib/ui-spec-cli.cjs
      provides: "cmdUiSpec(cwd, args, raw) — the `ui spec validate` arm, exit 1 on failure, exit 0 on ok; `render` is added by 34-05"
    - path: plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/
      provides: "six further known-broken specs, one per remaining invariant (proposal §17)"
  wiring:
    - "df-tools.cjs `case 'ui'` -> cmdUiSpec: a new `spec` subcommand beside the existing `metrics`, following the dispatch shape at df-tools.cjs:451-462."
    - "cmdUiSpec -> ui-spec.cjs parseSurfaceSpec + ui-spec-validate.cjs validateSurfaceSpec: the CLI is a thin arm over the two library functions, holding no rules of its own."
  key_links:
    - "Exit code 1 on invalid. The plan's W1b gate for this row is literally `df-tools ui spec validate <file>` exit 1 with codes. If the arm exits 0 on a broken spec, every consumer gate downstream (34-08's refusal to compose, W1*'s Phase A) silently passes."
    - "`process.exitCode`, not `process.exit()` — helpers.cjs `output()` always exits 0, so the arm must set the code and let stdout flush, the way flutter-ui-eval.cjs:649 does."
---

# TRD 34-04: invariants I4-I8, six more known-broken fixtures, and the `ui spec validate` arm

<objective>
Complete proposal §4.5's static invariant set — **I4 (states, seeds, outage≠empty), I5
(patterns and inherited `must_not` defaults), I6 (hit rects), I7 (flows), I8 (guards)** — each
with its own code and its own hand-built known-broken fixture, and expose the whole validator as
`df-tools ui spec validate <file>`, **exit 1 with codes** on failure.

**This TRD contains one `checkpoint:decision`.** I6's static half has no statically-evaluable
input in §4.2 (see the checkpoint task). It is resolved by a recorded decision before it is
implemented, never by a guess.

Resolved intent: kind=plugin, work=feature, tdd=strict, depth=comprehensive,
test_list_first=required, fixture_strategy=generators. Constraints honored:
`no_llm_test_data`, `no_property_based_default`, `no_gherkin_layer`.
</objective>

## Runtime model

Binding for this TRD (OBJECTIVE.md; proposal §21 amendment 2). Trace every command under it.

- **Mirror, not checkout.** Skills invoke `node ~/.claude/devflow/bin/df-tools.cjs` — the MIRROR
  at `~/.claude/devflow`, copied from `plugins/devflow/devflow/` by the `sync-runtime` hook.
  **This TRD adds a df-tools arm, so the mirror matters directly:** the arm this TRD ships is
  not reachable from a skill until the next SessionStart syncs the mirror. Tests must invoke the
  LOCAL `plugins/devflow/devflow/bin/df-tools.cjs` by absolute path
  (precedent: `verifier-ui-eval-invocation.test.cjs:42`), never `~/.claude/devflow/bin/…`, which
  would silently test the previously-installed engine.
- **Bash tool semantics.** cwd PERSISTS across Bash calls; shell variables do NOT. One plain
  command per call under the worktree guard; `( cd "$DIR" && … )` subshells; evidence paths
  absolute from a per-call `REPO_ROOT`.
- **CommonJS only.** `.cjs`, synchronous `fs`, adjacent tests. **No new npm dependencies.**
- **Honest output.** Every result carries `engine_version` + `schema_version`. A check that did
  not run reports `MISSING`, never `pass` — this TRD has two such cases: an unresolvable
  cross-surface entry (34-03) and a pattern list the engine cannot reach (I5, below).

<context>
- **Depends on 34-03.** Read `34-03-SUMMARY.md` first: it carries the shipped code table (do not
  collide with it), the behaviour-coverage model, and the fixture-hygiene loop this TRD's six new
  fixtures must satisfy unchanged.
- **TDD is STRICT.** Test list first, one test at a time, RED by exit code before GREEN,
  `test:` -> `feat:` commits. Commit the RED test before implementing.
- **Suite baseline (measured 2026-09-22):** `npm test` = **2974 pass / 9 fail / 50 skipped**,
  all 9 pre-existing (`handoff-e2e`, `devflow-watch`, `awareness`). **`npm test` is not a gate.**
  Re-measure at TRD start.
- **`autonomous: false`** — task 1 is a `checkpoint:decision`. Do not proceed past it.
- **Scope fence.** `ui spec render` is 34-05, `ui sheet` is 34-06, `ui lock` is 34-07. This TRD
  adds only the `validate` subcommand, and structures `ui-spec-cli.cjs` so 34-05 and 34-06 add
  theirs beside it without re-dispatching.
</context>

<test_list>
Behaviour cases, written before any test code. Outside-in this time the ordering is real: the
CLI arm is the outermost user-observable layer, so its cases (C1-C4) are written FIRST and drive
the library cases beneath them.

**The CLI arm (outermost)**
- **C1** — `df-tools ui spec validate __fixtures__/ui-spec/projects-rail.md` exits **0** and
  prints JSON with `ok: true`, a non-empty `engine_version`, and `schema_version: 1`.
- **C2** — the same arm on `broken/route-without-back.md` exits **1** and prints
  `ok: false` with `errors[0].code === 'ROUTE002'`. Exit 1 is the plan's literal gate.
- **C3** — a path that does not exist exits **1** with a one-line stderr error and no stack
  trace. A spec whose front matter fails to parse exits **1** with `SPEC000` and the yaml-lite
  line number.
- **C4** — `df-tools ui spec <unknown-subcommand>` and `df-tools ui <unknown>` each exit 1 with
  a message listing the available subcommands, matching the existing
  `error('Unknown ui subcommand. Available: metrics')` shape at `df-tools.cjs:459`.

**I4 — states, seeds, and the outage/empty distinction**
- **I4a** — a state with no `seed` -> exactly `STATE001` (`state-without-seed.md`).
- **I4b** — `outage.content.must_show` deep-equals `empty.content.must_show` -> exactly
  `STATE002` (`outage-equals-empty.md`). §4.4: *"the schema rejects a spec where they are equal"*.
- **I4c** — a NON-empty intersection that is not full equality (outage shows
  `["unavailable","Try again"]`, empty shows `["Create a project","Try again"]`) -> also
  `STATE002`. The plan's RED case is written as `outage.must_show ∩ empty.must_show ≠ ∅`, which
  is stricter than §4.4's "equal"; implement the intersection rule and pin both halves.
- **I4d** — a spec missing `dark` from its states -> exactly `STATE003`, message naming `dark`.
  §4.4's minimum set: populated, empty, error, outage, long-content, narrow, dark.
- **I4e** — `loading` is NOT required (§4.4: declared only when the surface owns an async
  fetch). Pin the negative so I4d does not over-fire.

**I5 — patterns**
- **I5a** — `patterns: [navigation/does-not-exist]` -> exactly `PAT001` (`unknown-pattern.md`).
- **I5b** — a control whose `kind` maps to a pattern that declares
  `must_not: ["cover sibling hit rects"]`, where the control's own `must_not` omits it ->
  exactly `PAT002` (§4.5 I5: *"a surface may not silently drop them"*).
- **I5c** — **the MISSING case.** When the caller supplies no pattern catalogue
  (`ctx.patterns === undefined`, i.e. no pinned `eden-ui-flutter` release is reachable), I5
  reports a `PAT000` entry with `status: 'MISSING'` and does NOT report `PAT001`/`PAT002`.
  An unreachable catalogue must never read as "patterns check passed" — the objective's
  standing rule. Distinguish `undefined` (unreachable) from `[]` (reachable and empty, which
  DOES produce `PAT001` for any referenced pattern).

**I6 — hit rects** (`hit-rect-overlap.md`)
- **I6a** — written AFTER the checkpoint in task 1, against the model it records. The fixture's
  `<!-- BROKEN: -->` comment cites the checkpoint decision by name.
- **I6b** — the positive control, whose header declares
  `hit_rect: {disjoint_from: [rail.project.chevron]}` and whose chevron declares the reciprocal,
  produces NO `HIT` error under the chosen model.

**I7 — flows**
- **I7a** — a flow step referencing a control or route id that does not exist -> exactly
  `FLOW001`, `path` naming the flow id and the step index.
- **I7b** — a flow whose last step is neither a `back` nor a declared terminal route -> exactly
  `FLOW002` (`flow-ends-mid-route.md`). "Terminal route" = a route with `root: true` or one
  carrying an explicit `terminal: true`; state which in the SUMMARY.
- **I7c** — the positive control's `open-project-conversation` flow, which ends in
  `{back: app-back, …}`, produces no `FLOW` error.

**I8 — guards**
- **I8a** — `guards: [member-of-workspace]` with no state whose `as`/`guard` marks it as that
  guard's denied state -> exactly `GUARD001` (`guard-without-denied-state.md`).
- **I8b** — the positive control, whose `guard-denied` state carries `as: non-member`,
  produces no `GUARD` error. Pin the linkage rule used (which field ties a guard to its denied
  state) in the SUMMARY — 34-05's nav graph draws a guard edge from the same rule.

**Hygiene (inherited)**
- **H1** — 34-03's I3h loop, unchanged, now over ELEVEN fixtures: each fails with exactly one
  code, and that code equals the one named in its `<!-- BROKEN: -->` comment.

Explicitly NOT in this TRD: rendering, the sheet, the lock, the skill prose, the harness.
</test_list>

<embedded_context>

<codebase_examples>
**df-tools dispatch** — `df-tools.cjs:451-462`, the existing `ui` case. Extend it, do not add a
second top-level case:

```js
    case 'ui': {
      const subcommand = args[1];
      if (subcommand === 'metrics') {
        cmdUiMetrics(cwd, args.slice(2), raw);
      } else {
        error('Unknown ui subcommand. Available: metrics');
      }
      break;
    }
```

becomes `metrics | spec` here, and 34-06/34-07 add `sheet | lock`. Keep the `error(...)`
message in sync with what is actually wired — C4 asserts it.

**Non-zero exit with JSON on stdout** — `flutter-ui-eval.cjs:627-649`. Read the comment there
before writing the arm; it is the exact trap this arm must avoid:

```js
  // `process.exitCode` (not `process.exit()`) is used … lets Node exit naturally once the
  // stream flushes
  process.exitCode = verdict === 'fail' ? 1 : 0;
```

`helpers.cjs`'s `output()` calls `process.exit(0)` unconditionally — so the arm must write
stdout itself (or set `process.exitCode` before calling a writer that does not exit). Do not
"fix" `output()`; a dozen callers depend on its exit-0 behaviour.

**Executing the real CLI from a test** — `verifier-ui-eval-invocation.test.cjs:38-42` resolves
`path.join(__dirname, '..', 'df-tools.cjs')` and runs it with `execSync`, catching the throw so
the assertion is about the payload and the exit code rather than about `execSync`'s behaviour.
Copy that pattern for C1-C4, including reading `e.status` for the exit code.
</codebase_examples>

<gotchas>
- **`output()` exits 0.** This is the single most likely way to ship a gate that never gates.
  C2 executes the real binary and asserts `status === 1`; a unit test on the library alone
  would not catch it.
- **`∩ ≠ ∅` vs "equal".** The plan says intersection; §4.4 says equal. Intersection is strictly
  stronger and satisfies both. Implement intersection, pin both I4b and I4c, and say so in the
  SUMMARY — this is a reconciliation, not a choice between the documents.
- **`undefined` patterns ≠ `[]` patterns.** I5c. The first means "I could not reach the pinned
  release" and must report `MISSING`; the second means "the release declares no patterns" and a
  referenced pattern is genuinely unknown. Collapsing them turns an unreachable catalogue into a
  silent pass, which is the exact class §2 goal 5 forbids.
- **`STATE003` must list ALL missing states in one error**, not one error per state — otherwise a
  new spec with two states produces five errors and the fixture-hygiene loop (H1) goes red for
  every fixture that happens to be minimal.
- **Six new fixtures, one edit each.** Same discipline as 34-03: `cp projects-rail.md`, change
  exactly one thing, add the `<!-- BROKEN: … -->` comment. `diff` against the positive control
  must show one hunk.
- **`ui-spec-cli.cjs` is a separate file from `ui-spec-validate.cjs`.** 34-05, 34-06 and 34-07
  all add arms; keeping the CLI plumbing in its own module means four TRDs edit one small file
  instead of four TRDs editing the validator.
</gotchas>

<anti_patterns>
- **Do NOT guess I6.** Task 1 is a checkpoint for exactly this reason. Implementing a hit-rect
  model and discovering at W1★ that the app cannot express it costs a spec-schema revision
  across two repos.
- **Do NOT put validation rules in the CLI arm.** The arm parses argv, calls the two library
  functions, prints, and sets an exit code. Every rule lives in `ui-spec-validate.cjs` where the
  fixtures can reach it.
- **Do NOT use `process.exit(1)`** to signal an invalid spec — stdout may not have flushed.
  `process.exitCode = 1` and return.
- **Do NOT let `PAT000/MISSING` set `ok: false`.** A check that could not run is not a failure;
  it is a MISSING row that stays on the human-verify list. `ok` reflects real violations. State
  the rule in the module header so 34-08's "refuse to compose" reads it correctly.
- **Do NOT reuse 34-03's codes.** Append: `STATE001-003`, `PAT000-002`, `HIT001`, `FLOW001-002`,
  `GUARD001`.
</anti_patterns>

<error_recovery>
- If H1 goes red after adding a fixture, `diff` it against `projects-rail.md`. More than one
  hunk means the fixture trips a second invariant; shrink the edit.
- If C2 exits 0, you have called `output()`. Re-read `flutter-ui-eval.cjs:627-649` and write
  stdout directly.
- If the positive control starts failing a NEW invariant, the invariant is wrong or the §4.2
  transcription is incomplete — diagnose which before editing either. Never relax V1.
- If `PAT001` fires on the positive control, `ctx.patterns` is `[]` where it should be the two
  pattern ids the fixture references. Check the test's `ctx()` helper, not the implementation.
</error_recovery>

</embedded_context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>DECIDE: what "overlapping hit rects" means statically (invariant I6)</name>
  <action>
**Do not implement I6 until this is answered. Do not guess.**

Present this to the user and record the answer in `34-04-SUMMARY.md` and in the
`hit-rect-overlap.md` fixture's `<!-- BROKEN: -->` comment.

**The conflict.** Proposal §4.5 invariant 6 reads: *"no two controls declare overlapping hit
rects unless one lists the other in `disjoint_from` (which the probe then asserts)"*. The plan's
W1b RED-case list names a fixture "overlapping `hit_rect` without `disjoint_from`". But §4.2's
schema gives `hit_rect` exactly one field — `{disjoint_from: [<control-id>]}` — and no
coordinates. §7.5 then defines the runtime `disjoint` check as *"rects of `disjoint_from` pairs
overlap"*, i.e. `disjoint_from` is an **assertion the probe verifies**, not a declaration of
overlap. **There is therefore no field in §4.2 from which a static overlap can be computed.**
I6 as written is not statically evaluable against the schema as written.

**Three candidate resolutions.** Each is implementable in this TRD; they differ in what the spec
author has to write and in what W2's probe inherits.

- **(a) Declared containment.** Add an optional `hit_rect.within: <control-id>` to the schema.
  Declaring one control inside another IS the static overlap. `HIT001` fires when
  `A.hit_rect.within === B.id` and neither A nor B lists the other in `disjoint_from`. Fixture:
  the chevron declares `within: rail.project.header` and the header drops its `disjoint_from`.
  *Cost:* one schema field. *Benefit:* the nav defect this program exists to catch (a row-sized
  chevron node inside a header) becomes statically declarable and the probe gets a rect pair to
  assert. Recommended.
- **(b) Reciprocity only.** No schema change. I6's static half becomes "every `disjoint_from`
  reference resolves to an existing control AND is reciprocal" — `HIT001` fires when A lists B
  but B does not list A, or when the target does not exist. Fixture: delete the chevron's
  reciprocal entry. *Cost:* the invariant no longer matches its §4.5 wording; the real overlap
  check is deferred entirely to W2's probe. *Benefit:* zero schema churn.
- **(c) Defer.** Implement no I6, ship `hit_rect` as schema-declared-but-unchecked, and have the
  validator emit `HIT000 / MISSING` for every surface. *Cost:* the plan's fixture list loses one
  entry; W1★ dogfoods without the check. *Benefit:* nothing is invented.

**Ask the user which, and note that (a) and (b) can co-exist** — (b)'s reciprocity check is
cheap and correct under (a) as well.

Whichever is chosen, `HIT000` (MISSING) remains the answer for any surface where the check could
not run. A hit-rect check that did not run never reports `pass`.
  </action>
  <verify>
The decision is recorded verbatim in `34-04-SUMMARY.md` under a `## I6 decision` heading, with
the chosen option letter, the user's reasoning, and — if (a) — the exact new schema field and
its `description`. `hit-rect-overlap.md` cites it.
  </verify>
  <done>
A named, recorded decision exists. I6a/I6b are written against it, not around it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>RED then GREEN: the CLI arm — exit 1 with codes</name>
  <files>plugins/devflow/devflow/bin/lib/ui-spec-cli.cjs, plugins/devflow/devflow/bin/lib/ui-spec-cli.test.cjs, plugins/devflow/devflow/bin/df-tools.cjs</files>
  <action>
Outermost layer first (CLAUDE.md habit 5). Write C1-C4 against the REAL binary, watch them fail,
then wire the arm.

The test resolves the LOCAL binary and reads the exit code from the throw:

```js
const DF_TOOLS = path.join(__dirname, '..', 'df-tools.cjs');   // NOT ~/.claude/devflow/bin
function runArm(args, cwd) {
  try { return { status: 0, stdout: execSync(`node ${DF_TOOLS} ${args}`, {cwd, encoding:'utf-8'}) }; }
  catch (e) { return { status: e.status, stdout: e.stdout, stderr: e.stderr }; }
}
```

Then `ui-spec-cli.cjs`:

```
cmdUiSpec(cwd, args, raw):
  sub = args[0]                     // 'validate'  (34-05 adds 'render')
  file = resolve(cwd, args[1])
  if (!sub || sub !== 'validate') error('Unknown ui spec subcommand. Available: validate')
  if (!file || !exists) error(`spec not found: ${args[1]}`)          // exit 1, one line, no stack
  { frontMatter } = parseSurfaceSpec(read(file))                      // SPEC000 on throw
  result = validateSurfaceSpec(frontMatter, { patterns: <catalogue or undefined>, vocabulary })
  process.stdout.write(JSON.stringify({ ...result, spec: file }, null, 2))
  process.exitCode = result.ok ? 0 : 1
```

   # CRITICAL: process.exitCode, NOT process.exit(), and NOT helpers.output() — output() calls
   #   process.exit(0) unconditionally and would make this gate structurally incapable of
   #   failing. See flutter-ui-eval.cjs:627-649 for the same decision and its reasoning.
   # CRITICAL: the pattern catalogue is `undefined` when unreachable, `[]` when reachable and
   #   empty. For W1b there is no pinned eden-ui-flutter release to read, so pass `undefined`
   #   and let I5 report MISSING. Do not pass `[]` "for now" — that would report PAT001 on
   #   every real spec.
   # PATTERN: df-tools dispatch per df-tools.cjs:451-462; extend the message to
   #   'Available: metrics, spec'.

Register in `df-tools.cjs` and commit `test:` then `feat:`.
  </action>
  <verify>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test ui-spec-cli.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
exits 0 with C1-C4 green.

**The gate contract, executed against the real binary — this is the plan's literal W1b gate:**
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node ../df-tools.cjs ui spec validate __fixtures__/ui-spec/projects-rail.md > /dev/null; echo "positive exit=$?"'
```
prints `positive exit=0`.
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node ../df-tools.cjs ui spec validate __fixtures__/ui-spec/broken/route-without-back.md > /dev/null 2>&1; echo "broken exit=$?"'
```
prints `broken exit=1`.
  </verify>
  <done>
`df-tools ui spec validate` exists, exits 0 on the positive control and 1 with codes on a broken
spec, and is reachable from the real binary — not only from a unit test.
  </done>
</task>

<task type="auto" tdd="true">
  <name>I4, I5, I7, I8 and (per the checkpoint) I6 — six known-broken fixtures</name>
  <files>plugins/devflow/devflow/bin/lib/ui-spec-validate.cjs, plugins/devflow/devflow/bin/lib/ui-spec-validate.test.cjs, plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/state-without-seed.md, plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/outage-equals-empty.md, plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/unknown-pattern.md, plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/hit-rect-overlap.md, plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/flow-ends-mid-route.md, plugins/devflow/devflow/bin/lib/__fixtures__/ui-spec/broken/guard-without-denied-state.md</files>
  <action>
Add I4a-I4e, I5a-I5c, I6a-I6b, I7a-I7c, I8a-I8b and H1 **one at a time**, each RED before GREEN,
`test:` -> `feat:` pairs. Write the fixture, observe the expected code, then implement.

**Codes added by this task** (appending to 34-03's table — do not collide):

```
STATE001  a state with no `seed`
STATE002  outage.content.must_show ∩ empty.content.must_show ≠ ∅
STATE003  §4.4's minimum state set incomplete (ONE error listing every missing state)
PAT000    pattern catalogue unreachable -> status MISSING, does NOT set ok:false
PAT001    a referenced pattern is not in the catalogue
PAT002    a control of a pattern kind drops one of that pattern's must_not defaults
HIT000    hit-rect check could not run -> MISSING
HIT001    per the task-1 checkpoint decision
FLOW001   a flow step names a control or route that does not exist
FLOW002   a flow's last step is neither a `back` nor a declared terminal route
GUARD001  a route guard names no denied state
```

**Fixtures** — `cp projects-rail.md broken/<name>.md`, ONE edit, `<!-- BROKEN: … -->` comment:

| fixture | edit | expected |
|---|---|---|
| `state-without-seed.md` | delete `seed:` from the `error` state | `STATE001` |
| `outage-equals-empty.md` | set `outage.content.must_show` to `["Create a project"]` | `STATE002` |
| `unknown-pattern.md` | `patterns: [navigation/does-not-exist]` | `PAT001` |
| `hit-rect-overlap.md` | per the checkpoint decision | `HIT001` |
| `flow-ends-mid-route.md` | delete the flow's final `{back: app-back, …}` step | `FLOW002` |
| `guard-without-denied-state.md` | delete the `guard-denied` state, leaving `guards:` in place | `GUARD001` |

Note `guard-without-denied-state.md` will also trip `CTRL006` if `guard-denied` appears in any
`visible_in` — check and, if so, remove it from that list in the SAME edit and say so in the
comment. H1 requires exactly one code.

   # CRITICAL: STATE003 is ONE error listing every missing state, not one per state. H1 depends
   #   on it.
   # CRITICAL: PAT000/HIT000 carry status MISSING and do NOT set ok:false. A check that did not
   #   run is not a pass and is not a failure. State the rule in the module header — 34-08's
   #   "refuse to compose without a valid spec" reads `ok`, and a MISSING pattern catalogue must
   #   not block composition on every surface.
   # GOTCHA: distinguish ctx.patterns === undefined (MISSING) from [] (empty catalogue). I5c.
   # GOTCHA: intersection, not equality, for STATE002 — reconciles the plan's `∩ ≠ ∅` with
   #   §4.4's "equal". I4b and I4c pin both halves.

Finish by re-running H1 (34-03's hygiene loop) over all ELEVEN fixtures.
  </action>
  <verify>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test ui-spec-validate.test.cjs ui-spec-cli.test.cjs ui-spec.test.cjs yaml-lite.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
exits 0, `fail 0`.

**Eleven-fixture §17 probe — run through the REAL CLI arm so exit codes are proven too:**
```
bash -c 'cd plugins/devflow/devflow/bin/lib && bad=0; for f in __fixtures__/ui-spec/broken/*.md; do out=$(node ../df-tools.cjs ui spec validate "$f" 2>/dev/null); st=$?; codes=$(printf "%s" "$out" | node -e "let s=\"\";process.stdin.on(\"data\",d=>s+=d).on(\"end\",()=>{const r=JSON.parse(s);console.log([...new Set(r.errors.filter(e=>!/000$/.test(e.code)).map(e=>e.code))].join(\",\"))})"); echo "$(basename $f) exit=$st codes=$codes"; [ "$st" = "1" ] || bad=1; case "$codes" in *,*) bad=1;; "") bad=1;; esac; done; exit $bad'
```
prints one line per fixture — `exit=1` and exactly one code each — and exits 0.

**Positive control through the real arm:**
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node ../df-tools.cjs ui spec validate __fixtures__/ui-spec/projects-rail.md | head -20; exit ${PIPESTATUS[0]}'
```
exits 0 and shows `"ok": true` with `engine_version` and `schema_version`.
  </verify>
  <done>
All eight §4.5 invariants are implemented. Eleven known-broken fixtures each exit 1 with exactly
one code through the real CLI. The positive control exits 0. `PAT000`/`HIT000` report MISSING
without setting `ok: false`.
  </done>
</task>

</tasks>

<verification>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test ui-spec-validate.test.cjs ui-spec-cli.test.cjs ui-spec.test.cjs yaml-lite.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
Exit code 0 is the evidence.

The eleven-fixture probe and the positive-control probe in task 3 `<verify>` are the plan's W1b
gate for this row (`df-tools ui spec validate <file>` exit 1 with codes) and belong in the
SUMMARY verbatim.

**Not a gate:** `npm test` (9 pre-existing failures — see `<context>`).
</verification>

<success_criteria>
- Proposal §4.5 invariants I1-I8 are all implemented, each with its own code.
- Eleven known-broken fixtures, one per invariant, each failing with exactly one code (§17).
- `df-tools ui spec validate <file>` exits 1 with codes on an invalid spec and 0 on the
  `projects-rail` example — proven by executing the real binary, not by a library unit test.
- Checks that could not run report MISSING and do not flip `ok`.
- The I6 decision is recorded, not guessed.
- No new npm dependency.
</success_criteria>

<output>
On completion write `34-04-SUMMARY.md` containing:
- `## I6 decision` — the checkpoint answer verbatim, with the option chosen, the reasoning, and
  any schema field added. W2's `disjoint` check inherits it.
- The complete shipped code table (34-03's plus this TRD's), one line each.
- The verbatim output of the eleven-fixture probe, including exit codes.
- The STATE002 reconciliation (intersection vs equality) stated explicitly.
- The `PAT000`/`HIT000` MISSING semantics and the rule that they do not flip `ok` — 34-08's
  refusal logic and W2's verifier replay both read it.
- The guard→denied-state linkage rule as implemented (which field ties them). 34-05 draws the
  nav graph's guard edge from the same rule.
- The terminal-route rule used by FLOW002 (`root: true`, or an explicit `terminal: true`).
- The re-measured `npm test` baseline and the verbatim RED output from task 2.
</output>
