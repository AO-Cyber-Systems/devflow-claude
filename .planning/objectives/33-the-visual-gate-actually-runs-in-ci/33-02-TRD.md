---
objective: 33-the-visual-gate-actually-runs-in-ci
trd: "02"
type: tdd
wave: 2
depends_on: ["33-01"]
files_modified:
  - plugins/devflow/devflow/bin/lib/verifier-ui-eval-invocation.test.cjs
  - plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs
  - plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs
autonomous: true
fixture_strategy: generators
requirements:
  - "UIEVAL-33-04: the invocation Step 8c actually contains resolves to something the engine can load"
  - "UIEVAL-33-05: verify flutter-ui-eval accepts an objective id, and still accepts a manifest path unchanged"
  - "UIEVAL-33-06: the handler emits the resolution status so callers can distinguish absent, invalid, not_applicable and resolved"
must_haves:
  truths:
    - "A test reads `agents/verifier.md`, EXTRACTS the `df-tools.cjs verify flutter-ui-eval …` command from Step 8c's bash block, substitutes `$OBJECTIVE` with a hermetic fixture objective id, EXECUTES it, and asserts the engine loaded something. It pins behaviour, not a string."
    - "That test was OBSERVED to fail against the current form, returning verbatim `{\"error\":\"manifest/captureResults not found\",\"path\":\"<id>\"}` — the RED is recorded in the SUMMARY."
    - "After the fix, the extracted invocation against a fixture objective WITH a manifest returns `resolution: 'resolved'` and a scoreRun rollup carrying a `verdict`."
    - "The extracted invocation against a fixture objective with UI TRDs and NO manifest returns `resolution: 'absent'` and exit code 0 — it runs, and says what it found."
    - "`verify flutter-ui-eval <manifest-path>` behaves exactly as before: every pre-existing dogfood case passes unchanged, and no #68 / objective-32 caller is disturbed."
    - "A differential control was performed in its INVERTED form (OBJECTIVE.md records why): reverting the handler to path-only resolution makes the extracted-invocation case fail BY NAME with the legacy payload, and restoring it leaves `git diff` empty."
  artifacts:
    - path: plugins/devflow/devflow/bin/lib/verifier-ui-eval-invocation.test.cjs
      provides: "Case V1 (the load-bearing extracted-invocation case), V2 (resolved + rollup), V3 (absent, exit 0), V4 (extraction guard: the bash block still contains exactly one such invocation)"
    - path: plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs
      provides: "cmdVerifyFlutterUIEval delegates its first positional argument to resolveUIEvalTarget and emits `resolution` alongside the rollup; path arguments unchanged"
    - path: plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs
      provides: "Case D-ID: the CLI accepts an objective id at the same arm the workflow uses (`flutter-ui eval`), proving both arms benefit"
  wiring:
    - "cmdVerifyFlutterUIEval -> resolveUIEvalTarget: the handler stops doing its own fs.existsSync/path.join resolution and calls the module 33-01 built. One implementation, one place."
    - "resolution status -> output(): `not_applicable` / `absent` / `invalid` reach the caller as named statuses on an `ok: true` envelope, not as `{ error }`, so 33-03 can route them apart."
  key_links:
    - "verifier.md Step 8c bash block -> the executing test: the only mechanism that can pin an agent-prose instruction to a behaviour. If the extraction breaks, the test must fail loudly (Case V4), never silently pass on zero matches."
    - "explicit-path arm -> unchanged behaviour: the back-compat seam that keeps PR #68 and objective 32's four TRDs working."
---

# TRD 33-02: The invocation Step 8c actually contains resolves to something the engine can load

<objective>
Make the gate reachable, and prove it with a test that executes the invocation **extracted
from `verifier.md` itself** rather than a string copied into a test file. Then wire
`cmdVerifyFlutterUIEval` to 33-01's `resolveUIEvalTarget` so that objective ids work,
manifest paths keep working, and the three non-scoring outcomes arrive as named statuses
instead of one collapsed `{ error }`.

This is the load-bearing TRD. Everything before it is preparation; everything after it is
routing.

Resolved intent: kind=plugin, work=bugfix, tdd=strict (CLAUDE.md user playbook),
depth=comprehensive, fixture_strategy=generators. Constraints honored: no_llm_test_data
(hand-built temp objective trees + the checked-in fixture manifest), no_property_based_default
(descriptive named cases), no_gherkin_layer.
</objective>

<context>
- **Branch:** `fix/ui-eval-gate-runs`. See OBJECTIVE.md "Branch decision". This TRD edits
  `flutter-ui-eval.cjs` — the file objective 32 edits in all four of its TRDs — which is
  precisely why 33 is stacked on 32's branch rather than run in parallel.
- **Objective 32 lands first.** Before starting, confirm 32's changes are present:
  `grep -n "unjudged" plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs` should hit
  (32-01), and `grep -n "gate:" ...` should show `advisory`/`binding` (32-03). If they do
  not, you are on the wrong base — stop and re-cut the branch.
- **TDD is STRICT (Iron Law).** Test-list first (below), **one test at a time**
  RED → GREEN → REFACTOR. Commit the RED test BEFORE implementing — `maxTurns: 50` is a
  hard ceiling and a committed RED test survives truncation.
- **Outside-in ordering** (CLAUDE.md habit 5): V1 is written at the outermost layer that
  exists — the real CLI, invoked with the real command string taken out of the real agent
  prompt. That is as close to "what CI would run" as this repo can get without a CI job.
- Repo conventions: CommonJS `.cjs`, synchronous `fs`, tests adjacent to source, Node's
  built-in runner (`node --test`). **No new dependencies.**
</context>

<test_list>
Behaviour cases for this TRD, written before any test code (CLAUDE.md habit 2).
Ordered outside-in.

**The load-bearing case — write this FIRST, watch it FAIL:**
- **V1 (CLI, extracted from prose)** — read `plugins/devflow/agents/verifier.md`, pull the
  `verify flutter-ui-eval` command out of Step 8c's bash block, substitute `$OBJECTIVE`
  with a hermetic fixture objective id, run it with `cwd` set to the fixture root, and
  assert the result is **not** the legacy `{ error: 'manifest/captureResults not found' }`
  and carries a `resolution` in `{not_applicable, absent, invalid, resolved}`.

**Then the outcomes that invocation can reach:**
- **V2** — fixture objective with a `type: ui` + `stack: flutter` TRD **and** a manifest at
  `evidence/ui_eval/manifest.json` → `resolution: 'resolved'` plus a real scoreRun rollup
  (`verdict` present). *This is the sentence "something the engine can load", asserted.*
- **V3** — fixture objective with UI TRDs and **no** manifest → `resolution: 'absent'`,
  process exit code **0**, and `searched[]` non-empty. It runs and reports; it does not
  crash and does not silently vanish.
- **V4 (extraction guard)** — the extraction itself found **exactly one**
  `verify flutter-ui-eval` invocation in Step 8c. If Step 8c is renamed, reflowed, or the
  invocation is removed, V4 fails loudly rather than V1 silently passing on zero matches.
  *A test that can pass by finding nothing is not a test.*

**Back-compat (must stay green, must NOT be weakened):**
- **B1** — `verify flutter-ui-eval <checked-in fixture manifest> --raw` produces the same
  rollup as before this TRD (whatever objective 32 left it as — capture the pre-TRD output
  and diff against it).
- **B2** — every pre-existing dogfood case (D1, D2, F1, DF1, DF2, DF3, N1, plus objective
  32's additions) passes unchanged.
- **D-ID** — the `flutter-ui eval <objective-id>` arm resolves too, not just
  `verify flutter-ui-eval`. Both arms route to the same handler; assert it, because the
  workflow uses the former.

Explicitly NOT in this TRD's test list: the routing policy for `absent`/`invalid`, the
Step 8c prose rewrite, the `visual_gate` ratchet — all 33-03.
</test_list>

<embedded_context>

<codebase_examples>
**The prose to extract from** — `plugins/devflow/agents/verifier.md:551-554`, verbatim:

````markdown
```bash
UI_EVAL=$(node ~/.claude/devflow/bin/df-tools.cjs verify flutter-ui-eval "$OBJECTIVE" --raw)
```
````

The extraction must survive reasonable reflow. A workable shape:

```js
const md = fs.readFileSync(VERIFIER_MD, 'utf-8');
// Narrow to Step 8c so a match in Step 8d's design-review block can't be mistaken for it.
const step8c = md.slice(md.indexOf('### Step 8c'), md.indexOf('### Step 8d'));
const matches = [...step8c.matchAll(/df-tools\.cjs\s+verify\s+flutter-ui-eval\s+([^\n)]*)/g)];
assert.strictEqual(matches.length, 1, 'Step 8c must contain exactly one verify flutter-ui-eval invocation');
const argTail = matches[0][1].trim();               // e.g.  "$OBJECTIVE" --raw
const argv = argTail.replace(/"\$OBJECTIVE"|\$OBJECTIVE/g, FIXTURE_OBJECTIVE_ID);
execSync(`node ${DF_TOOLS} verify flutter-ui-eval ${argv}`, { cwd: fixtureRoot, encoding: 'utf-8' });
```

# CRITICAL: run the LOCAL `df-tools.cjs` (path.join(__dirname, '..', 'df-tools.cjs')), not
#   the `~/.claude/devflow/bin/` path the prose contains. The prose path points at the
#   SYNCED plugin cache, which lags this worktree by a release. Substitute the binary path,
#   keep the ARGUMENTS — the arguments are what this objective is about.

**The handler to change** — `flutter-ui-eval.cjs:529-551`. The two lines that make the gate
unreachable:

```js
  const absManifest = path.isAbsolute(manifestArg) ? manifestArg : path.join(cwd, manifestArg);
  if (!fs.existsSync(absManifest)) {
    output({ error: 'manifest/captureResults not found', path: manifestArg, ok: false }, raw);
    return;
  }

  let manifest;
  try {
    manifest = loadManifest(absManifest);
  } catch (e) {
    output({ error: 'invalid manifest: ' + e.message, path: absManifest, ok: false }, raw);
    return;
  }
```

Both blocks are replaced by a single `resolveUIEvalTarget(cwd, manifestArg)` call plus a
switch on `resolution`. Note the existing code already distinguishes not-found from
invalid — it just reports both as `{ error, ok: false }`, which Step 8c flattens to SKIPPED.
Preserve the distinction and surface it.

**The status vocabulary precedent** — `flutter-state-coverage.cjs:240`:

```js
    output({ status: 'not_applicable', reason: 'not a Flutter UI TRD', trd_path: absTrd }, raw);
```

**The dogfood harness to extend** — `flutter-ui-eval-dogfood.test.cjs:22-28`:

```js
function runRaw(argStr, opts = {}) {
  return execSync(`node ${DF_TOOLS} ${argStr}`, { encoding: 'utf-8', ...opts });
}
function runJSON(argStr) { return JSON.parse(runRaw(`${argStr} --raw`)); }
```
`runRaw` already forwards `opts`, so `{ cwd: fixtureRoot }` works without changing it.
</codebase_examples>

<gotchas>
- **`execSync` throws on a non-zero exit.** Objective 32-04 makes a `fail` verdict exit
  non-zero. V2's fixture manifest must therefore be one that scores clean, or V2 must catch
  and read `e.stdout`. Build V2's fixture from the checked-in `good-dashboard` state plus a
  matching `labels.json` so it scores `pass` — otherwise V2 fails for a reason that has
  nothing to do with this TRD.
- **The `absent` and `not_applicable` paths must exit 0.** Step 8c's standing contract is
  "NEVER a hard fail", and 32-04 only makes *judged failures* exit non-zero. If a missing
  manifest starts exiting non-zero, every non-UI objective's verification breaks. V3 pins
  the exit code explicitly.
- **`output()` shape matters.** Emitting `{ status: 'absent', ok: false }` would make Step 8c
  read it as an error and skip — reproducing the bug through a new field name. The
  non-scoring statuses are `ok: true` with a `resolution`; only a genuinely broken
  invocation (no argument at all) stays `ok: false`.
- **Do not change the `--help` output shape** — dogfood Case D1 matches on it.
- **Require cycle.** `flutter-ui-eval.cjs` will require `flutter-ui-eval-resolve.cjs`, which
  lazily requires `loadManifest` back from `flutter-ui-eval.cjs`. 33-01 was told to keep that
  require **inside the function** for exactly this reason. If you see `loadManifest is not a
  function` at runtime, a top-level require crept in — move it back inside.
- **`path` argument still means path.** Tier 1 of the resolver is "the argument names an
  existing file". Do not add a heuristic that inspects the string for digits or extensions;
  `verify flutter-ui-eval 33` and `verify flutter-ui-eval ./33.json` must both do the
  obvious thing, and existence on disk is what separates them.
- `${PIPESTATUS[0]}` in a `verify` block: `$?` after a pipe reports the LAST command. Use
  `set -o pipefail` or `${PIPESTATUS[0]}` **inside a `bash -c '...'` wrapper** — this repo
  has a CI guard that fails on the broken form, and the interactive shell here is zsh (where
  the array is `pipestatus`), so always wrap in `bash -c`.
</gotchas>

<anti_patterns>
- **Do NOT copy the command string into the test.** The entire value of V1 is that it reads
  the command out of `verifier.md`. A hard-coded copy re-creates the drift this objective
  exists to close, and would pass forever while Step 8c said something else.
- **Do NOT let V1 pass when the extraction finds nothing.** That is what V4 is for. A regex
  that matches zero times and an assertion that iterates zero times is a green test proving
  nothing.
- **Do NOT edit `verifier.md` in this TRD.** Under decision (c) the invocation is already
  correct; the handler is what changes. Editing Step 8c here would also collide with
  objective 32-03 and muddy the differential control. Step 8c prose is 33-03.
- **Do NOT weaken any existing dogfood case** to accommodate the new output field. If one
  breaks, it is asserting on the exact rollup shape — add the field alongside, do not remove
  what is there. If a test genuinely encoded the old error payload as correct behaviour,
  change it deliberately and say so in the SUMMARY with the reasoning.
- No new dependencies. Node built-ins only.
</anti_patterns>

<error_recovery>
- If V1's `execSync` throws `ENOENT`, the substituted binary path is wrong — you kept the
  prose's `~/.claude/devflow/bin/` path instead of the local one. Substitute the binary,
  keep the arguments.
- If the extraction regex matches the Step 8d design-review command too, your slice bound is
  wrong. Slice strictly between `### Step 8c` and `### Step 8d`.
- If the whole test file fails to load, that is a `SyntaxError`, not a RED test. Read the
  runner output before concluding anything.
- If the dogfood suite goes red in a way you did not expect after wiring, revert the handler
  change (`git checkout -- plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs`), confirm
  green, and re-apply in smaller pieces — resolution first, output shape second.
</error_recovery>

</embedded_context>

<tasks>

<task type="auto" tdd="true">
  <name>RED: the load-bearing case — execute the invocation Step 8c actually contains</name>
  <files>plugins/devflow/devflow/bin/lib/verifier-ui-eval-invocation.test.cjs</files>
  <action>
Create the file and write Cases V1 and V4 ONLY (V4 first — it guards V1). Do not write V2/V3
yet. Do not touch `flutter-ui-eval.cjs` in this task.

1. Build the fixture with a hand-written factory (fixture-builder work, CLAUDE.md habit 4 —
   no generated data):

```js
// makeFixtureObjective({ id, slug, ui: true, manifest: null })
//   -> { root, objectiveId }  under fs.mkdtempSync(path.join(os.tmpdir(), 'verifier-ui-eval-'))
//   writes <root>/.planning/objectives/<id>-<slug>/<id>-01-TRD.md with
//     type: ui  /  stack: flutter  frontmatter
```
   Register `after()` cleanup with `fs.rmSync(root, { recursive: true, force: true })`.

2. **V4 (write first)** — slice `verifier.md` between `### Step 8c` and `### Step 8d`,
   `matchAll` the `verify flutter-ui-eval` invocations, assert **exactly one**. Message:
   "Step 8c must contain exactly one `verify flutter-ui-eval` invocation — V1 cannot pin
   what it cannot find."

3. **V1** — take V4's single match, substitute `$OBJECTIVE` (quoted or bare) with the
   fixture objective id, substitute the binary path with the local `df-tools.cjs`, and
   `execSync` it with `cwd: root`. Parse the JSON and assert:
   - it is NOT `{ error: 'manifest/captureResults not found' }` — message:
     "Step 8c's invocation must resolve to something the engine can load (objective 33)"
   - `typeof parsed.resolution === 'string'` and it is one of
     `not_applicable | absent | invalid | resolved`
   Catch a non-zero exit and read `e.stdout` so the assertion message is about the payload,
   not about `execSync`.

4. RUN and RECORD the failure verbatim:

    bash -c 'cd plugins/devflow/devflow/bin/lib && node --test verifier-ui-eval-invocation.test.cjs 2>&1 | tail -40; exit ${PIPESTATUS[0]}'

   Expected pre-fix output, reproduced during planning:
   `{"error":"manifest/captureResults not found","path":"<fixture-id>","ok":false}` with
   exit code 0. Paste it into the commit body — this is the evidence that the gate has never
   run.

5. COMMIT THE RED TEST NOW, before implementing:

    node ~/.claude/devflow/bin/df-tools.cjs commit "test(ui-eval): RED — Step 8c's own invocation resolves to nothing loadable (obj 33)" --files plugins/devflow/devflow/bin/lib/verifier-ui-eval-invocation.test.cjs
  </action>
  <verify>
`bash -c 'cd plugins/devflow/devflow/bin/lib && node --test verifier-ui-eval-invocation.test.cjs 2>&1 | tail -40; exit ${PIPESTATUS[0]}'`
exits NON-ZERO. **V4 PASSES** (the invocation was found — exactly one) and **V1 FAILS** with
the "must resolve to something the engine can load" message. If V4 fails, the extraction is
broken and V1's red is meaningless — fix V4 first.
`git log -1 --stat` shows the RED test committed and `flutter-ui-eval.cjs` unchanged.
  </verify>
  <done>
V1 and V4 exist, are committed, and V1 has been OBSERVED to fail with the legacy
`manifest/captureResults not found` payload recorded verbatim.
  </done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: the handler accepts an objective id and reports which of the four outcomes it hit</name>
  <files>plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs, plugins/devflow/devflow/bin/lib/verifier-ui-eval-invocation.test.cjs, plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs</files>
  <action>
Make V1 green with the smallest honest change, then add V2, V3 and D-ID one at a time (RED
then GREEN for each — do not batch).

1. In `cmdVerifyFlutterUIEval`, replace the `fs.existsSync` + `loadManifest` try/catch pair
   (`:545-560`) with:

```js
const { resolveUIEvalTarget } = require('./flutter-ui-eval-resolve.cjs');
const target = resolveUIEvalTarget(cwd, manifestArg);

if (target.resolution !== 'resolved') {
  // not_applicable | absent | invalid — named, ok:true, exit 0. NOT an { error }.
  output({
    resolution: target.resolution,
    reason: target.reason,
    objective_dir: target.objective_dir,
    manifest_path: target.manifest_path,
    searched: target.searched,
    ui_trds: target.ui_trds,
    visual_gate: target.visual_gate,
    ok: true,
  }, raw);
  return;
}
const absManifest = target.manifest_path;
const manifest = target.manifest;   // resolver already loaded+validated it
```

   # CRITICAL: `ok: true` on the three non-scoring statuses. `{ ok: false }` is how Step 8c
   #   decides something errored, and 33-03 needs these three routed APART, not flattened
   #   into one SKIPPED branch. Only a missing argument stays `ok: false`.
   # CRITICAL: exit code stays 0 on not_applicable / absent / invalid. Objective 32-04 makes
   #   a JUDGED failure exit non-zero; an unrun gate is not a judged failure. V3 pins this.
   # PATTERN: `status`/`not_applicable` vocabulary copied from flutter-state-coverage.cjs:240.
   # GOTCHA: keep the resolver's loadManifest require lazy (33-01) or you get a require cycle.

2. Add `resolution: 'resolved'` to the SUCCESS rollup too, alongside `manifest_path`. Both
   branches then answer the same question, and 33-03 can switch on one field.

3. Leave everything downstream of `const manifest = ...` untouched — the judge selection,
   the offline/live split, `scoreRun`, and objective 32's `gate` / `unjudged` fields are not
   this TRD's business.

4. Add **V2** (fixture objective + manifest → `resolved` + a `verdict`) and **V3** (fixture
   objective, no manifest → `absent`, exit 0, `searched[]` non-empty), each RED first. Build
   V2's manifest from the checked-in `good-dashboard` state with a matching `labels.json` so
   it scores clean and `execSync` does not throw.

5. Add **D-ID** to `flutter-ui-eval-dogfood.test.cjs`: `flutter-ui eval <objective-id> --raw`
   resolves too — both CLI arms route to the same handler, and the workflow uses the second
   one.

Commit as `test:` → `feat:` pairs (2-3 atomic commits across this task is correct).
  </action>
  <verify>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test verifier-ui-eval-invocation.test.cjs flutter-ui-eval.test.cjs flutter-ui-eval-dogfood.test.cjs flutter-ui-eval-resolve.test.cjs flutter-ui-eval-planner-default.test.cjs flutter-ui-eval-bootstrap.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
exits 0, `fail 0`, with V1-V4 and D-ID present.

The reproduction from the objective brief, now expected to be unreachable:
```
bash -c 'node plugins/devflow/devflow/bin/df-tools.cjs verify flutter-ui-eval 33 --raw; echo "EXIT=$?"'
```
must print a `resolution` (`not_applicable` for objective 33 itself — it has no `type: ui`
TRDs) and `EXIT=0`. It must NOT print `manifest/captureResults not found`.

Back-compat (B1/B2), run explicitly:
```
bash -c 'node plugins/devflow/devflow/bin/df-tools.cjs verify flutter-ui-eval plugins/devflow/devflow/bin/lib/__fixtures__/flutter-ui-eval/manifest.json --raw'
```
produces the same rollup as before this TRD, now with `"resolution": "resolved"` added.
Diff it against the output you captured before starting.
  </verify>
  <done>
An objective id resolves. A manifest path still resolves exactly as before. The three
non-scoring outcomes arrive as named `ok: true` statuses at exit 0. V1 is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Differential control (inverted form): prove V1 catches the original defect by name</name>
  <files>plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs</files>
  <action>
A test that has never been seen to fail is not evidence. Task 1's RED proves V1 fails against
the ORIGINAL code; this proves it fails against a REINTRODUCTION of the exact defect, by
name, after the fix is in.

**Read OBJECTIVE.md's "Differential control" section first.** The brief's form (restore the
`"$OBJECTIVE"` string in `verifier.md`) is INVERTED by the resolution-ownership decision:
`"$OBJECTIVE"` is now the *correct* form. The defect now lives on the handler, so the control
is performed there.

1. Temporarily revert the handler to path-only resolution — restore, verbatim:

```js
  const absManifest = path.isAbsolute(manifestArg) ? manifestArg : path.join(cwd, manifestArg);
  if (!fs.existsSync(absManifest)) {
    output({ error: 'manifest/captureResults not found', path: manifestArg, ok: false }, raw);
    return;
  }
```

2. Run and RECORD the output verbatim:
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test verifier-ui-eval-invocation.test.cjs flutter-ui-eval-dogfood.test.cjs 2>&1 | tail -40; exit ${PIPESTATUS[0]}'
```
   Confirm it exits non-zero AND that **Case V1 is named** in the failure output AND that the
   failure payload is the legacy `{"error":"manifest/captureResults not found",...}`. If the
   suite fails for some other reason, or V1 is NOT among the failures, the control has NOT
   passed — the test is not actually pinned to this defect. Fix the test, do not proceed.

3. Restore the fix: `git checkout -- plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs`.

4. Confirm the working tree is clean for that file:
```
bash -c 'git diff --stat -- plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs; git diff --quiet -- plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs && echo CLEAN || echo DIRTY'
```
   must print `CLEAN` with no diff output.

5. Re-run the suite: green again.

Record the differential-control transcript (the named V1 failure with the legacy payload +
the CLEAN confirmation) in the TRD SUMMARY. There is nothing to commit in this task — a
commit here would mean the restore failed.
  </action>
  <verify>
The SUMMARY contains: (a) the recorded failure output with Case V1 named and the legacy
`manifest/captureResults not found` payload visible; (b) `CLEAN` from the `git diff --quiet`
check; (c) the green re-run.
`git status --short plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs` produces no output.
  </verify>
  <done>
The differential control passed in its inverted form: the defect was reintroduced on the
handler, Case V1 failed by name with the legacy payload, the fix was restored, and
`git diff` for the file is empty.
  </done>
</task>

</tasks>

<verification>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test verifier-ui-eval-invocation.test.cjs flutter-ui-eval.test.cjs flutter-ui-eval-dogfood.test.cjs flutter-ui-eval-resolve.test.cjs flutter-ui-eval-planner-default.test.cjs flutter-ui-eval-bootstrap.test.cjs flutter-ui-scope.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
Exit code 0 is the evidence.

The objective's headline claim, asserted at the CLI:
```
bash -c 'node plugins/devflow/devflow/bin/df-tools.cjs verify flutter-ui-eval 33 --raw | grep -q manifest/captureResults && { echo "STILL UNREACHABLE"; exit 1; } || echo "GATE REACHABLE"; exit 0'
```
must print `GATE REACHABLE`.
</verification>

<success_criteria>
- SC-1 (objective) — an objective id resolves; the legacy error payload is unreachable for a
  valid id.
- SC-2 (objective) — a manifest path argument is untouched; dogfood green.
- SC-7 (objective) — the load-bearing test executes the invocation extracted from
  `verifier.md`; RED observed and recorded.
- SC-8 (objective) — differential control performed in its inverted form; `git diff` empty.
</success_criteria>

<output>
On completion write `33-02-SUMMARY.md` containing:
- The recorded RED output from task 1 (verbatim, including the legacy payload).
- The differential-control transcript from task 3 (the named V1 failure + `CLEAN`).
- The exact extraction mechanism used (the slice bounds and the regex), so 33-03 knows what
  reflowing Step 8c will and will not break.
- The pre-TRD vs post-TRD rollup diff for the checked-in fixture manifest (B1 evidence).
- Any existing test that had to change, with the reasoning for why it encoded the old
  behaviour as correct.
</output>
