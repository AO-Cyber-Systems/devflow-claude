---
objective: 33-the-visual-gate-actually-runs-in-ci
trd: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.cjs
  - plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.test.cjs
autonomous: true
fixture_strategy: generators
requirements:
  - "UIEVAL-33-01: an objective id resolves to its ui-eval manifest through one documented lookup order, implemented once, in code"
  - "UIEVAL-33-02: an objective with zero type:ui + stack:flutter TRDs resolves to not_applicable"
  - "UIEVAL-33-03: absent, invalid and resolved are four distinguishable statuses, never one collapsed error"
must_haves:
  truths:
    - "`resolveUIEvalTarget(cwd, '33')` returns `{ resolution: 'not_applicable' }` when the objective has no TRD with `type: ui` AND `stack: flutter` — the condition verifier Step 8c already gates on."
    - "`resolveUIEvalTarget(cwd, '33')` returns `{ resolution: 'absent', searched: [...] }` when UI TRDs exist but no manifest is present at any lookup location, and `searched[]` names every location it looked."
    - "A manifest found at `.planning/objectives/<obj>/evidence/ui_eval/manifest.json` returns `{ resolution: 'resolved', manifest_path }` pointing at that file."
    - "A manifest that exists but is unparseable returns `{ resolution: 'invalid', reason }` — distinguishable from `absent` by status, not merely by message text."
    - "A `.yaml`/`.yml` manifest with no `.json` sibling returns `{ resolution: 'invalid', reason: 'yaml-manifest-unsupported' }` rather than being silently ignored as absent."
    - "An argument that exists on disk as a file is returned verbatim as `{ resolution: 'resolved' }` — the path caller's behaviour is preserved exactly."
    - "The module reads the filesystem and nothing else: no network, no new dependency, no YAML parser."
  artifacts:
    - path: plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.cjs
      provides: "resolveUIEvalTarget(cwd, arg) — the single implementation of the objective-id -> manifest lookup, with applicability detection and four distinguishable statuses"
    - path: plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.test.cjs
      provides: "Cases A1-A4 (applicability) and M1-M7 (lookup precedence, absent, invalid, yaml) over hermetic temp .planning trees"
  wiring:
    - "resolveUIEvalTarget -> objective.cjs findObjectiveInternal: objective-id -> directory reuses the existing resolver rather than re-globbing .planning/objectives."
    - "resolveUIEvalTarget -> frontmatter.cjs extractFrontmatter: the type:ui + stack:flutter scan reuses the existing parser, mirroring flutter-state-coverage.cjs:233-240."
  key_links:
    - "applicability scan -> not_applicable: the branch that preserves Step 8c's existing silent skip for non-Flutter objectives. If this regresses, every objective in the repo starts producing gate output."
    - "absent vs invalid: the distinction that stops a broken manifest from hiding behind a missing one."
---

# TRD 33-01: One lookup, in code — objective id to manifest, with four honest statuses

<objective>
Build `resolveUIEvalTarget(cwd, arg)`: the single, tested implementation of the ui-eval
manifest lookup. It answers two questions the current handler cannot answer at all —
"is this objective even a Flutter UI objective?" and "where is its manifest, and if there
isn't one, is that absence or breakage?" — and returns four distinguishable statuses.

This TRD builds and unit-tests the module in isolation. **It wires nothing.** The handler
change is 33-02; the verifier routing is 33-03. This wave must end with the suite green.

Per OBJECTIVE.md's resolution-ownership decision (option c, implemented as b): resolution
lives in code, once, so the two prose documents that already drifted apart have nothing
left to drift about.

Resolved intent: kind=plugin, work=bugfix, tdd=strict (CLAUDE.md user playbook),
depth=comprehensive, fixture_strategy=generators. Constraints honored: no_llm_test_data
(hand-built temp `.planning` trees), no_property_based_default (descriptive named cases),
no_gherkin_layer.
</objective>

<context>
- **Branch:** `fix/ui-eval-gate-runs`, cut from `fix/ui-eval-default-honesty` at its tip
  (or from `feat/ui-visual-eval` if objective 32 has already merged there). **Never from
  `main`.** See OBJECTIVE.md "Branch decision".
- **This TRD creates a NEW file.** It does not touch `flutter-ui-eval.cjs`, so it cannot
  conflict with objective 32's four TRDs. That is deliberate — the conflict surface is
  confined to 33-02 and 33-03.
- **TDD is STRICT (Iron Law).** Test-list first (below), **one test at a time**
  RED → GREEN → REFACTOR, hand-built fixtures only. Commit the RED test BEFORE
  implementing — executors have a hard `maxTurns: 50` ceiling, and a committed RED test
  means a ceiling hit truncates progress rather than erasing it.
- **Outside-in ordering** (CLAUDE.md habit 5) does not apply cleanly here: this is a pure
  filesystem-resolution unit with no user-observable layer of its own. The outermost layer
  (the CLI, and the invocation extracted from `verifier.md`) is 33-02's load-bearing case.
  Order these unit cases applicability-first, because `not_applicable` short-circuits
  everything else.
- Repo conventions: CommonJS `.cjs`, synchronous `fs`, tests adjacent to source, Node's
  built-in runner (`node --test`). **No new dependencies.**
- Re-baseline before starting: objective 32 adds cases to the four ui-eval test files.
  Record the count you actually observe; do not assert the planning-time figure of 85.
</context>

<test_list>
Behaviour cases for this TRD, written before any test code (CLAUDE.md habit 2).
Every case builds its own hermetic temp tree with `fs.mkdtempSync` — nothing reads the
real `.planning/`.

**Applicability (write these FIRST — `not_applicable` short-circuits the rest):**
- **A1** — temp objective dir with two TRDs, both `type: standard` → `resolution: 'not_applicable'`.
- **A2** — temp objective dir with one TRD carrying `type: ui` AND `stack: flutter` →
  NOT `not_applicable` (it proceeds to manifest lookup).
- **A3** — `type: ui` but `stack: react` (or `stack` absent) → `not_applicable`. Both
  conditions are required, matching `flutter-state-coverage.cjs:239`.
- **A4** — an objective id that matches no directory at all → `resolution: 'not_applicable'`
  with a `reason` naming the unknown objective. **Never a throw** — Step 8c must never
  hard-fail (its own contract), so this module must never throw at its callers.

**Manifest lookup (only reached when applicable):**
- **M1** — argument is an existing file path → `resolution: 'resolved'`, `manifest_path`
  is that file, and NO objective lookup was attempted. (Preserves every current caller.)
- **M2** — `.planning/objectives/<obj>/evidence/ui_eval/manifest.json` exists →
  `resolution: 'resolved'` pointing at it.
- **M3** — no objective-local manifest, but `ui_eval/manifests/web.manifest.json` exists at
  the repo root → `resolution: 'resolved'` pointing at it. Tier 3 also searches
  `flutter/ui_eval/manifests/*.manifest.json`.
- **M4** — precedence: BOTH M2's and M3's locations exist → M2 wins, and M3's file is
  reported in `additional_candidates[]` (visible, not silently discarded).
- **M5** — applicable objective, no manifest anywhere → `resolution: 'absent'` and
  `searched[]` lists every location that was checked, in order.
- **M6** — `manifest.json` exists but contains `{ not json` → `resolution: 'invalid'`, with
  the parse reason. **Asserted distinguishable from M5 by `resolution`**, not by message.
- **M7** — only `manifest.yaml` exists (no `.json` sibling) → `resolution: 'invalid'`,
  `reason: 'yaml-manifest-unsupported'`, and `manifest_path` names the `.yaml` file so the
  operator can see what was found. Never `absent` — the file is right there.

**Regression guard (must stay green):**
- **R1** — the module is `require`-able with zero side effects and adds no dependency:
  `Object.keys(require('./flutter-ui-eval-resolve.cjs'))` contains `resolveUIEvalTarget`
  and the file's `require(...)` list is limited to node builtins plus `./objective.cjs`
  and `./frontmatter.cjs`.

Explicitly NOT in this TRD's test list: the CLI handler (33-02), the invocation extracted
from `verifier.md` (33-02), the routing policy and Step 8c prose (33-03).
</test_list>

<embedded_context>

<codebase_examples>
**The applicability check to mirror** — `flutter-state-coverage.cjs:233-240`, which already
answers "is this a Flutter UI TRD?" and already uses the `not_applicable` vocabulary. Reuse
both the parser and the word; do not invent a second spelling:

```js
  const { extractFrontmatter } = require('./frontmatter.cjs');
  // ...
  // Only applicable to Flutter UI TRDs
  if (fm.type !== 'ui' || fm.stack !== 'flutter') {
    output({ status: 'not_applicable', reason: 'not a Flutter UI TRD', trd_path: absTrd }, raw);
```

**The objective-id → directory resolver to reuse** — `objective.cjs:89`, exported as
`findObjectiveInternal`. It normalises the id, searches `.planning/objectives`, then falls
back to archived milestone directories, and returns `{ directory, jobs: [...TRD filenames] }`.
Do NOT re-glob `.planning/objectives` by hand:

```js
function findObjectiveInternal(cwd, objective) {
  if (!objective) return null;
  const objectivesDir = path.join(cwd, '.planning', 'objectives');
  const normalized = normalizeObjectiveName(objective);
  const current = searchObjectiveInDir(objectivesDir, path.join('.planning', 'objectives'), normalized);
  if (current) return current;
  // ... archived milestones ...
}
```

`searchObjectiveInDir` returns `jobs` (the TRD filenames) already sorted — that is the list
to scan for `type: ui` + `stack: flutter`.

**What tier 3 actually contains** — `flutter-ui-eval-bootstrap.cjs:34`:

```js
const MANIFEST_REL = path.join('ui_eval', 'manifests', 'web.manifest.json');
```

Note this is `ui_eval/manifests/*.manifest.json` relative to the **project dir**, which is
the repo root in a Flutter repo and `flutter/` in a mixed repo. `workflows/ui-eval.md:34`
documents it as `flutter/ui_eval/manifests/*.yaml` — wrong extension AND wrong prefix.
Search both prefixes; the prose gets corrected in 33-03.

**What the engine can load** — `flutter-ui-eval.cjs:57`:

```js
function loadManifest(manifestPath) {
  const raw = fs.readFileSync(manifestPath, 'utf-8');
  const parsed = JSON.parse(raw);           // throws SyntaxError on invalid JSON
  if (parsed === null || typeof parsed !== 'object' || Array.isArray(parsed)) throw ...
  if (!Array.isArray(parsed.states)) throw new Error("manifest missing required 'states' array");
  return parsed;
}
```

`invalid` must cover **all three** failure modes — unreadable, non-JSON, and structurally
wrong (no `states` array). Reuse `loadManifest` for the validation rather than
re-implementing the shape guard; catch its throw and convert it to `{ resolution: 'invalid' }`.
</codebase_examples>

<gotchas>
- **This module must NEVER throw.** Step 8c's standing contract is "NEVER a hard fail", and
  33-03 keeps that promise for `not_applicable`. If `resolveUIEvalTarget` throws on a
  missing `.planning/` directory, an unreadable file, or a garbage id, the promise breaks at
  the layer below the one that made it. Wrap every filesystem read. Case A4 pins this.
- **`fm.type !== 'ui' || fm.stack !== 'flutter'` requires BOTH.** A TRD with `type: ui` and
  no `stack` is not a Flutter UI TRD. Case A3 pins it. Getting this wrong makes every UI-ish
  objective in every repo start emitting gate output.
- **`searchObjectiveInDir` matches by prefix** (`d.startsWith(normalized)`), so the id `3`
  matches `33-...` before `3-...` depending on sort order. Pass the id through unchanged and
  let `findObjectiveInternal` own that behaviour — do not add a second normalisation. If a
  test needs an exact objective, use the full directory name.
- **Tier-3 glob order must be deterministic.** `fs.readdirSync` order is not guaranteed
  across platforms. Sort the matches lexicographically before picking `[0]`, or M3/M4 will
  be flaky on someone else's machine.
- **`fs.existsSync(arg)` is the path-vs-id discriminator, not a regex.** Do not sniff for
  `.json` in the string: `verify flutter-ui-eval 33` and
  `verify flutter-ui-eval ./manifest.json` differ by whether the argument names a file that
  exists, relative to `cwd`. Resolve relative arguments against `cwd` exactly as
  `cmdVerifyFlutterUIEval:545` already does (`path.isAbsolute(arg) ? arg : path.join(cwd, arg)`).
- **A directory is not a manifest.** `fs.existsSync` is true for directories. Check
  `statSync().isFile()`, or an argument like `.planning` resolves as a "path" and then
  explodes in `readFileSync`.
- `${PIPESTATUS[0]}` in a `verify` block: `$?` after a pipe reports the LAST command. Use
  `set -o pipefail` or `${PIPESTATUS[0]}` **inside a `bash -c '...'` wrapper** — this repo
  has a CI guard that fails on the broken form, and the interactive shell here is zsh (where
  the array is `pipestatus`), so always wrap in `bash -c`.
</gotchas>

<anti_patterns>
- **Do NOT add a YAML parser.** No new dependencies. A `.yaml` manifest is `invalid` with an
  explicit reason (Case M7) — honest, testable, and a deliberate decision recorded in
  OBJECTIVE.md's out-of-scope list.
- **Do NOT collapse `absent` and `invalid` into one status with different message text.**
  That is today's bug wearing better prose. Case M6 asserts on `resolution`, not on the
  message.
- **Do NOT re-implement objective-id resolution or frontmatter parsing.** Both exist
  (`objective.cjs`, `frontmatter.cjs`). A second copy is how this objective's root cause
  happened in the first place.
- **Do NOT touch `flutter-ui-eval.cjs` in this TRD.** Objective 32 is editing it. The whole
  wave-1 design is that this file is new and conflict-free.
- Do NOT read the real repo `.planning/` in tests. Hermetic temp trees only — otherwise the
  suite's verdict changes every time someone plans an objective.
</anti_patterns>

<error_recovery>
- If `node --test` reports the whole file failing to load, you have a syntax error in the
  test file, not a RED test. Re-read the runner output for `SyntaxError` before assuming RED.
- If `findObjectiveInternal` returns `null` for a temp-tree objective you know exists, the
  temp tree is missing the `.planning/objectives` nesting — it resolves relative to the
  `cwd` you pass, not to `process.cwd()`. Build the full
  `<tmp>/.planning/objectives/<NN>-<slug>/` chain.
- If a tier-3 case passes locally and you suspect ordering luck, delete the sort and confirm
  it becomes flaky; that tells you the sort is load-bearing. Restore it.
</error_recovery>

</embedded_context>

<tasks>

<task type="auto" tdd="true">
  <name>RED: applicability — a non-UI objective must resolve to not_applicable, and nothing must throw</name>
  <files>plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.test.cjs</files>
  <action>
Write Cases A1-A4 ONLY, one at a time. Do not write M1-M7 yet. Do not create
`flutter-ui-eval-resolve.cjs` yet.

Build a hand-written fixture helper at the top of the test file (this is the fixture-builder
task per CLAUDE.md habit 4 — hand-built factory, no generated data):

```js
// makeObjectiveTree({ id, slug, trds: [{ name, frontmatter }], manifest, repoManifest })
//   -> { cwd, objectiveDir }   built under fs.mkdtempSync(path.join(os.tmpdir(), 'ui-eval-resolve-'))
```

It must write a real `<tmp>/.planning/objectives/<id>-<slug>/<name>-TRD.md` for each entry,
with a real `---` frontmatter block, because `findObjectiveInternal` and
`extractFrontmatter` both read from disk. Register an `after()` cleanup with
`fs.rmSync(tmp, { recursive: true, force: true })`.

Then the cases:

- **A1** — two TRDs, `type: standard` → `resolution === 'not_applicable'`.
- **A2** — one TRD with `type: ui` and `stack: flutter` → `resolution !== 'not_applicable'`
  (assert it is one of `absent | invalid | resolved`; A2 has no manifest so it will be
  `absent` once implemented — assert the negative here so the case survives 33-02).
- **A3** — `type: ui`, `stack: react` → `not_applicable`. Add a second sub-assertion for
  `type: ui` with `stack` absent entirely.
- **A4** — `resolveUIEvalTarget(tmp, 'no-such-objective')` → `not_applicable` with a
  `reason` mentioning the objective, and — the load-bearing half — **it does not throw**.
  Wrap in `assert.doesNotThrow`.

RUN and RECORD the failure verbatim:

    bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval-resolve.test.cjs 2>&1 | tail -30; exit ${PIPESTATUS[0]}'

Expected RED: `Cannot find module './flutter-ui-eval-resolve.cjs'` — the module does not
exist. That is a legitimate RED for a new module; record it verbatim.

COMMIT THE RED TEST NOW, before implementing — `maxTurns: 50` is a hard ceiling and a
committed RED test survives truncation:

    node ~/.claude/devflow/bin/df-tools.cjs commit "test(ui-eval): RED — objective-id applicability resolution (A1-A4)" --files plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.test.cjs
  </action>
  <verify>
`bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval-resolve.test.cjs 2>&1 | tail -30; exit ${PIPESTATUS[0]}'`
exits NON-ZERO and names the missing module. `git log -1 --stat` shows the RED test
committed. No other test file changed.
  </verify>
  <done>
Cases A1-A4 exist, are committed, and have been OBSERVED to fail with the output recorded.
`flutter-ui-eval-resolve.cjs` does not exist yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: resolveUIEvalTarget applicability half</name>
  <files>plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.cjs</files>
  <action>
Create `flutter-ui-eval-resolve.cjs` and make A1-A4 green with the smallest honest change.

Approach:

1. Module header comment in the house style (see `flutter-ui-eval-planner-default.cjs` for
   the shape): purpose, exports, anti-patterns avoided.
2. `resolveUIEvalTarget(cwd, arg)`:

```
1. If arg names an existing FILE (resolved against cwd) -> defer to the manifest half (task 3).
   For now: return { resolution: 'resolved', manifest_path: abs, source: 'explicit-path' }.
2. Otherwise treat arg as an objective id:
   info = findObjectiveInternal(cwd, arg)
   if (!info) return { resolution: 'not_applicable', reason: `objective '<arg>' not found` }
3. Scan info.jobs (TRD filenames) under info.directory:
   for each, extractFrontmatter -> fm
   applicable = fm.type === 'ui' && fm.stack === 'flutter'
   if none applicable -> { resolution: 'not_applicable', reason: 'no type:ui + stack:flutter TRD',
                           objective_dir: info.directory }
4. Applicable: return { resolution: 'absent', objective_dir, ui_trds: [...], searched: [] }
   (task 3 fills in the real lookup; 'absent' with an empty searched[] is the honest
   placeholder and keeps A2 green.)
```

   # CRITICAL: every fs call wrapped. This module must NEVER throw — Step 8c's contract is
   #   "NEVER a hard fail" and A4 pins it. A try/catch returning
   #   { resolution: 'not_applicable', reason: 'resolution error: ' + e.message } is correct;
   #   an escaping exception is not.
   # PATTERN: applicability test is `fm.type !== 'ui' || fm.stack !== 'flutter'`, copied from
   #   flutter-state-coverage.cjs:239. BOTH conditions required (A3).
   # GOTCHA: fs.existsSync is true for directories — use statSync().isFile() for the
   #   path-vs-id discriminator.

3. Carry `ui_trds: [...]` (the applicable TRD filenames) on the result. 33-03's routing
   policy needs to name the surfaces, and collecting them here costs nothing.
4. Also collect `visual_gate` from the applicable TRDs' frontmatter (`true` if ANY applicable
   TRD declares `visual_gate: true`). 33-03's ratchet reads it. Do not act on it here.
5. `module.exports = { resolveUIEvalTarget }`.

Commit as a `feat:` after A1-A4 are green.
  </action>
  <verify>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval-resolve.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
exits 0 with `fail 0` and 4+ passing cases.

Nothing else regressed:
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval.test.cjs flutter-ui-eval-dogfood.test.cjs flutter-ui-eval-planner-default.test.cjs flutter-ui-eval-bootstrap.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
exits 0 at the count you re-baselined at the start of this TRD.
  </verify>
  <done>
`resolveUIEvalTarget` exists, A1-A4 pass, the module never throws on a garbage id, and
`flutter-ui-eval.cjs` is untouched.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Manifest lookup: precedence, absent, invalid, and the yaml trap</name>
  <files>plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.cjs, plugins/devflow/devflow/bin/lib/flutter-ui-eval-resolve.test.cjs</files>
  <action>
Add M1-M7 and R1, **one test at a time**, each RED before GREEN. Do not batch.

Lookup order, in precedence order (this is the one implementation of it):

```
Tier 1  arg names an existing FILE            -> source: 'explicit-path'
Tier 2  <objective_dir>/evidence/ui_eval/manifest.json   -> source: 'objective-evidence'
Tier 3  <cwd>/ui_eval/manifests/*.manifest.json          -> source: 'repo-manifests'
        <cwd>/flutter/ui_eval/manifests/*.manifest.json  (both prefixes; sorted; first wins)
```

Every tier that was checked goes into `searched[]` in order, whether it hit or not — an
`absent` result must be able to tell the operator *where you should have put it*.

Validation, for whichever candidate wins:

```
try { loadManifest(candidate) }              // reuse flutter-ui-eval.cjs's loader
catch (e) { return { resolution: 'invalid', manifest_path: candidate, reason: e.message } }
return { resolution: 'resolved', manifest_path: candidate, source, searched, additional_candidates }
```

   # CRITICAL: reuse loadManifest — it already covers unreadable, non-JSON, and
   #   "missing required 'states' array". A second shape guard would be a second thing to
   #   drift. Requiring flutter-ui-eval.cjs from here creates no cycle (the dependency is
   #   one-directional; 33-02 will require THIS module from the handler, so keep the
   #   loadManifest require LAZY — inside the function — to avoid a require cycle when
   #   33-02 lands).
   # GOTCHA: sort tier-3 matches lexicographically before picking [0]. readdirSync order is
   #   not guaranteed and M3/M4 will be flaky on another platform.
   # GOTCHA: yaml. If tier 2 or tier 3 finds NO .json but a .yaml/.yml sibling exists at the
   #   same location, return { resolution: 'invalid', manifest_path: <the yaml>,
   #   reason: 'yaml-manifest-unsupported' }. Never 'absent' — the file is right there, and
   #   workflows/ui-eval.md currently tells people to write exactly that file (33-03 corrects
   #   the prose). Silently treating it as absent would recreate this objective's bug in a
   #   new location.

Cases, in order: M1 (explicit path wins, no objective lookup), M2 (tier 2 hit), M3 (tier 3
hit, both prefixes), M4 (precedence + `additional_candidates[]`), M5 (`absent` +
`searched[]` non-empty and ordered), M6 (`{ not json` → `invalid`, asserted
`!== 'absent'`), M7 (yaml-only → `invalid` + `reason: 'yaml-manifest-unsupported'`).

Then R1: assert the module's exports and that its `require` list contains no third-party
package (read the file and assert every `require('...')` argument starts with `node:`, is a
node builtin name, or starts with `./`).

Commit as `test:` → `feat:` pairs (2-3 atomic commits across this task is correct).
  </action>
  <verify>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval-resolve.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
exits 0, `fail 0`, with A1-A4 + M1-M7 + R1 present.

Distinguishability probe — the point of the whole TRD, run it explicitly:
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval-resolve.test.cjs 2>&1 | grep -E "M5|M6|M7"; exit 0'
```
shows M5 (`absent`), M6 (`invalid`) and M7 (`invalid`/yaml) all passing as separate cases.

Full group unchanged:
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval.test.cjs flutter-ui-eval-dogfood.test.cjs flutter-ui-eval-planner-default.test.cjs flutter-ui-eval-bootstrap.test.cjs flutter-ui-scope.test.cjs 2>&1 | tail -8; exit ${PIPESTATUS[0]}'
```
exits 0.
  </verify>
  <done>
The lookup order is implemented once, in code, with `absent`, `invalid` and `resolved`
distinguishable by status. A yaml-only manifest reports `invalid`, not `absent`. No new
dependency. `flutter-ui-eval.cjs` is still untouched by this TRD.
  </done>
</task>

</tasks>

<verification>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval-resolve.test.cjs flutter-ui-eval.test.cjs flutter-ui-eval-dogfood.test.cjs flutter-ui-eval-planner-default.test.cjs flutter-ui-eval-bootstrap.test.cjs flutter-ui-scope.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
Exit code 0 is the evidence. Expected delta: +12 or more cases (A1-A4, M1-M7, R1), `fail 0`,
and the pre-TRD count for the other five files unchanged.

Untouched-file proof (this TRD must not collide with objective 32):
```
bash -c 'git diff --name-only $(git merge-base HEAD @{u} 2>/dev/null || echo HEAD~3)..HEAD | sort'
```
must list only `flutter-ui-eval-resolve.cjs` and `flutter-ui-eval-resolve.test.cjs`.
</verification>

<success_criteria>
- SC-3 (objective) — four distinguishable statuses exist and are unit-tested.
- SC-4 (objective) — `not_applicable` is reachable and never throws, preserving Step 8c's
  silent skip for non-Flutter objectives.
- SC-6 (objective) — `invalid` is distinguishable from `absent` by status.
- No new dependency. `flutter-ui-eval.cjs` unmodified.
</success_criteria>

<output>
On completion write `33-01-SUMMARY.md` containing:
- The recorded RED output from task 1 (verbatim).
- The re-baselined test count observed at the start of the TRD (objective 32 will have
  moved it off the planning-time figure of 85).
- The exact lookup order as implemented, with the tier-3 prefixes actually searched.
- The `searched[]` shape for an `absent` result — 33-03 renders it into the MISSING note.
- Whether `visual_gate` was found on any real TRD in this repo (planning found none; the
  ratchet is expected dormant — record it so 33-03 does not assume otherwise).
</output>
