---
work: feature
---

# Program-aware TUI viewer

## Goal

Read-only terminal UI rendering parallel sessions + their position in the org tree. Composes obj 2 (peer awareness) + obj 5 (initiatives) + obj 6 (check-todos) + obj 1 (gh chain) into a single screen. tmux-pane friendly. Doesn't gate execution. Refresh on key press; auto-refresh disabled by default.

---
*Created: 2026-05-06 (auto-scaffold via bootstrapObjectiveMd)*
