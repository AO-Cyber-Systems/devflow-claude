---
objective: 10-flutter-ui-verification-process
trd: 10-02
title: Flutter state-pattern catalog (references/flutter-state-patterns.md)
type: standard
wave: 1
depends_on: []
files_modified:
  - plugins/devflow/devflow/references/flutter-state-patterns.md
autonomous: true
requirements: [REQ-10-02]
overrides:
  work: feature
  tdd: strict

must_haves:
  truths:
    - "references/flutter-state-patterns.md exists with regex catalog for three state-management libraries: Riverpod, flutter_bloc, and setState"
    - "Each pattern entry documents: name, regex pattern, states it covers, confidence level (HIGH/MEDIUM/LOW), and a note explaining false-positive/false-negative risk"
    - "The doc explicitly documents which patterns are BLOCKERS (HIGH confidence) vs ADVISORY (MEDIUM/LOW) — verifier (TRD 10-05) reads this to decide pass/fail vs warn"
    - "Riverpod section covers AsyncValue.when, AsyncValue.maybeWhen, switch-pattern matching on AsyncValue, whenData, and empty-state heuristics"
    - "flutter_bloc section covers BlocBuilder, sealed-class state names (Loading/Loaded/Error/Initial), and bloc_test usage signals"
    - "setState section covers isLoading boolean, error nullable, empty-check heuristics — all flagged as LOW or MEDIUM confidence"
    - "Doc includes a 'Per-TRD state aliases' section explaining how downstream TRDs can override regex per artifact (deferred to phase 2, but documented as the escape hatch)"
    - "Doc includes a 'Web verification mechanism' section explaining the flutter drive substitution model: web-tier black-box verification invokes the same tests.integration path that mobile uses, but via flutter drive --driver=test_driver/integration_test.dart --target=<tests.integration> -d chrome. Cites upstream blocker mobile-dev-inc/maestro#2591 as the rationale for why Maestro is mobile-only by design."
  artifacts:
    - path: "plugins/devflow/devflow/references/flutter-state-patterns.md"
      provides: "Regex catalog per state-management library for state coverage verification"
      contains: "AsyncValue"
      contains_also: ["BlocBuilder", "isLoading", "riverpod:", "bloc:", "setState:", "flutter drive"]
  key_links: []
---

<objective>
Author `plugins/devflow/devflow/references/flutter-state-patterns.md` — a static reference document consumed by the verifier (TRD 10-05) and planner (TRD 10-03). The catalog contains regex patterns per state-management library that map "state names" (e.g., `loading`, `data`, `error`, `empty`) to test-file presence checks.

Purpose: This catalog is the ground truth for "what does it mean for a widget test to cover state X under state-management library Y." Without this, the verifier's state-coverage grep has nothing to grep for. This TRD is independent of all other TRDs — it ships as a pure markdown reference.

Output: A single new reference doc (~280 lines markdown) with three regex catalog sections (Riverpod / Bloc / setState), confidence tagging per pattern, false-positive/false-negative profile table, the deferred "state aliases" escape hatch, and a "Web verification mechanism" section documenting the `flutter drive` substitution model.

Why standard (NOT tdd): This is a static reference document. There is no behavior to test-drive — the catalog is consumed at runtime by the verifier (TRD 10-05), which is where the tests for "does the regex match the right state in a real widget test file" live. Per CLAUDE.md TDD Playbook exception list: "config-only / pure docs."
</objective>

<file_tree>
plugins/devflow/devflow/references/
└── flutter-state-patterns.md       ← CREATE (new reference doc, ~280 lines)
</file_tree>

<execution_context>
@~/.claude/devflow/workflows/execute-trd.md
@~/.claude/devflow/templates/summary.md
</execution_context>

<embedded_context>

<codebase_examples>
<!-- Existing reference-doc style. From `references/tdd.md` and `references/anti-patterns.md`: -->

EXISTING reference docs use this structure:
1. Header (`# Title`) + one-paragraph summary of purpose
2. Section headers (`## Section`)
3. Tables, code blocks, and bulleted lists
4. Cross-references via `@` syntax to other references and templates
5. No frontmatter (these are static references, not TRDs)

EXAMPLE — `references/anti-patterns.md` structure:
```markdown
# Anti-Patterns Reference

When the planner agent generates TRDs, it should AVOID these patterns...

## Pattern: ...
**Why bad:** ...
**What to do instead:** ...
```

This TRD's output should follow the same conventions.
</codebase_examples>

<anti_patterns>
**Do NOT include regex patterns for libraries OUT OF SCOPE.** Patrol, get_it, Provider (basic), MobX, ScopedModel — NOT in scope per user constraint. Only Riverpod, flutter_bloc, and setState appear in the catalog.

**Do NOT mark every pattern as HIGH confidence.** RESEARCH.md is explicit: Riverpod `.when()` is HIGH, `.maybeWhen()` is LOW (ambiguous branches), setState boolean patterns are MEDIUM at best (high false-positive rate because `isLoading`/`isEmpty` are generic Dart-isms). Honest confidence labels are the whole point.

**Do NOT propose a Dart linter or custom Dart-side tooling.** Per RESEARCH.md "Don't Hand-Roll" table and "Anti-Patterns to Avoid": the catalog is grep'd from Node.js verifier-side. The regexes here ARE the implementation.

**Do NOT include golden-test or Patrol patterns.** Both out of scope.

**Do NOT document a `maestro_web: true` opt-in path.** Maestro is mobile-only BY DESIGN. The web-tier black-box verifier is `flutter drive` invoking the SAME `tests.integration` path that mobile uses via `flutter test`. There is no Maestro-on-web opt-in. The "Web verification mechanism" section documents the substitution model.
</anti_patterns>

<error_recovery>
If a regex appears wrong during later TRD execution (10-05 verifier wires it up), update this doc with a corrective entry — DO NOT silently fix in code. The doc IS the spec; tests in TRD 10-05 reference patterns BY NAME (see RESEARCH.md regex catalog YAML — patterns are named `when_all_three`, `bloc_loading_state`, etc.).
</error_recovery>

</embedded_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/objectives/10-flutter-ui-verification-process/10-RESEARCH.md
@plugins/devflow/devflow/references/anti-patterns.md
@plugins/devflow/devflow/references/tdd.md
</context>

<research_context>
The regex catalog content is fully specified in 10-RESEARCH.md "Code Examples" → "State Coverage Regex Catalog (REQ-10-02 reference content)". This TRD transcribes that content into a reference doc with the following structure:

1. **Title + Purpose** (1 paragraph) — explain "this catalog is consumed by the verifier to grep widget tests for state coverage."
2. **How it's used** (1 paragraph) — explain verifier-side: load the regex set for the TRD's `state_management:` field, then for each state in `must_haves.artifacts[*].states[]`, run the corresponding patterns against `must_haves.artifacts[*].tests.widget`. Patterns flagged HIGH are blockers; MEDIUM/LOW are advisories.
3. **Riverpod section** — 7 patterns from RESEARCH.md, each with confidence tag and note.
4. **flutter_bloc section** — 6 patterns from RESEARCH.md, each with confidence tag and note.
5. **setState section** — 5 patterns from RESEARCH.md, each with confidence tag and note.
6. **False-positive/false-negative profile table** — the matrix from RESEARCH.md verbatim.
7. **Web verification mechanism** — explains the `flutter drive` substitution model: `tests.integration` is invoked by both platforms (`flutter test` mobile, `flutter drive` web). `tests.maestro` is mobile-only-by-design due to upstream issue mobile-dev-inc/maestro#2591.
8. **Per-TRD state aliases (deferred, Phase 2)** — document the escape hatch shape for future use.

Confidence labels (from RESEARCH.md):
| Library | False Positives | False Negatives |
|---------|-----------------|-----------------|
| Riverpod | LOW (`AsyncValue.when` distinctive) | MEDIUM (`.maybeWhen` ambiguous) |
| Bloc | LOW (sealed-class state names distinctive) | LOW-MEDIUM (custom names won't match) |
| setState | HIGH (`isLoading`/`isEmpty` generic) | LOW |

User correction (2026-05-24): both mobile and web are required coverage. Web verification flows through `flutter drive`. Maestro is mobile-only-by-design due to mobile-dev-inc/maestro#2591.
</research_context>

<gotchas>
- All regex patterns must be JavaScript-compatible (consumed by Node-side grep in TRD 10-05). Use `\s`, `\(`, etc. — no PCRE-only constructs.
- Document HIGH-confidence-only as blockers per RESEARCH.md guidance. The verifier will downgrade MEDIUM/LOW results to advisory.
- The "Web verification mechanism" section is load-bearing: it tells the executor (TRD 10-04b) and verifier (TRD 10-05) WHY `tests.maestro` is mobile-only and HOW web verification happens. Future readers must understand this — link to mobile-dev-inc/maestro#2591 so they can check upstream status.
- The doc is consumed at RUN time (verifier reads it), not parse time. It does NOT need to be valid YAML — markdown with fenced code blocks containing the patterns is correct.
</gotchas>

<tasks>

<task type="auto">
  <name>Task 1: Create flutter-state-patterns.md reference doc with full catalog</name>
  <files>plugins/devflow/devflow/references/flutter-state-patterns.md</files>
  <action>
Create a new file at `plugins/devflow/devflow/references/flutter-state-patterns.md`. Transcribe the RESEARCH.md regex catalog into a self-contained reference doc.

Concrete structure:

```markdown
# Flutter State-Pattern Catalog

Regex catalog mapping abstract state names (`loading`, `data`, `error`, `empty`, `initial`) to JavaScript-compatible regex patterns per state-management library. **Consumed by the verifier (TRD 10-05)** to grep Flutter widget test files for state coverage. Also referenced by the planner (TRD 10-03) when detecting `state_management:` from imports.

## How it's used

When a TRD has `type: ui` + `stack: flutter`, each entry under `must_haves.artifacts[*]` declares:
- `state_management: riverpod | bloc | setState | other`
- `states: [loading, data, error, ...]`
- `tests.widget: <path>`

The verifier:

1. Loads the regex set matching the TRD's `state_management:` value.
2. For each state name in `states[]`, finds the corresponding regex entries (`covers: [<state>]`) in the catalog.
3. Runs the regex(es) against the contents of the `tests.widget` file.
4. Emits coverage result per state:
   - **HIGH confidence + regex match** → state coverage VERIFIED (blocker if missing).
   - **MEDIUM confidence + regex match** → state coverage PROBABLE (advisory — manual review recommended).
   - **LOW confidence + regex match** → state coverage POSSIBLE (advisory; high false-positive risk).
   - **No regex match** → state coverage MISSING (blocker for HIGH-confidence patterns; advisory for MEDIUM/LOW-only states).

Empty-state coverage is always advisory (empty is a UX concept, not a library primitive).

## Confidence model

| Tag | Verifier behavior |
|-----|-------------------|
| HIGH | Blocker on miss (state was declared but no matching pattern in widget test → verification FAIL) |
| MEDIUM | Advisory on miss; PASS with note "manual review recommended" |
| LOW | Advisory always; surface in VERIFICATION.md `notes:` not `gaps:` |

## Riverpod (`state_management: riverpod`)

Library: `flutter_riverpod`. Primary primitives: `AsyncValue`, `.when()`, `.maybeWhen()`, `.whenData()`, sealed-class pattern matching (Dart 3+).

```yaml
riverpod:
  - name: when_all_three
    pattern: '\.when\s*\(\s*loading\s*:\s*[^,]+,\s*data\s*:\s*[^,]+,\s*error\s*:'
    covers: [loading, data, error]
    confidence: HIGH
    note: "Canonical AsyncValue.when usage. Distinctive enough that false positives are rare."

  - name: when_skipped_loading
    pattern: '\.when\s*\(\s*data\s*:\s*[^,]+,\s*error\s*:\s*[^,]+,\s*skipLoadingOnReload\s*:\s*true'
    covers: [data, error]
    confidence: HIGH
    note: "AsyncValue.when variant that omits loading branch via skipLoadingOnReload flag."

  - name: maybe_when
    pattern: '\.maybeWhen\s*\('
    covers: []
    confidence: LOW
    note: "Ambiguous — does not declare which branches are present. Verifier should mark artifact PARTIAL and require manual review of branches when this is the only match."

  - name: pattern_match_switch_error
    pattern: 'switch\s*\([^)]+\)\s*\{[^}]*AsyncValue\s*\([^)]*hasError\s*:\s*true'
    covers: [error]
    confidence: MEDIUM
    note: "Dart 3 sealed-class pattern matching. Growing idiom 2026; less prevalent than .when()."

  - name: pattern_match_switch_loading
    pattern: 'switch\s*\([^)]+\)\s*\{[^}]*AsyncValue\s*\([^)]*isLoading\s*:\s*true'
    covers: [loading]
    confidence: MEDIUM
    note: "Pattern-match variant for loading state via isLoading boolean."

  - name: when_data_only
    pattern: '\.whenData\s*\('
    covers: [data]
    confidence: HIGH
    note: "AsyncValue.whenData — short-circuit for data-only handling. Distinctive."

  - name: empty_state_pattern
    pattern: '(isEmpty|\.isEmpty\b|length\s*==\s*0)'
    covers: [empty]
    confidence: MEDIUM
    note: "Empty is a UX concept, not a Riverpod primitive. Pattern catches the common Dart idiom but appears in non-state contexts too."
```

## flutter_bloc (`state_management: bloc`)

Library: `flutter_bloc`. Primary primitives: `BlocBuilder`, sealed-class State subclasses (`Loading`, `Loaded`, `Error`, `Initial`), `bloc_test` library.

```yaml
bloc:
  - name: bloc_builder_with_switch
    pattern: 'BlocBuilder<[^>]+>\s*\(\s*builder\s*:[^}]*switch\s*\([^)]+\)'
    covers: []
    confidence: MEDIUM
    note: "Signals that BlocBuilder uses switch-based state branching. Verifier should additionally grep for specific State subclass names within the switch (see Loading/Loaded/Error/Initial below)."

  - name: bloc_loading_state
    pattern: '(case\s+\w+Loading|is\s+\w+Loading\b|\bLoadingState\b)'
    covers: [loading]
    confidence: HIGH
    note: "Matches sealed-class Loading state references in switch branches or `is` checks."

  - name: bloc_loaded_data_state
    pattern: '(case\s+\w+(Loaded|Success|Data)|is\s+\w+(Loaded|Success|Data)\b)'
    covers: [data]
    confidence: HIGH
    note: "Matches Loaded/Success/Data state names. Custom domain names (e.g., LoggedIn) require per-TRD aliases (Phase 2)."

  - name: bloc_error_state
    pattern: '(case\s+\w+(Error|Failure)|is\s+\w+(Error|Failure)\b)'
    covers: [error]
    confidence: HIGH
    note: "Matches Error/Failure sealed-class state names."

  - name: bloc_initial_state
    pattern: '(case\s+\w+Initial|is\s+\w+Initial\b)'
    covers: [initial]
    confidence: HIGH
    note: "Matches Initial sealed-class state — required for full state-graph coverage in bloc."

  - name: bloc_test_used
    pattern: 'blocTest\s*\(|emitsInOrder\s*\(\['
    covers: []
    confidence: HIGH
    note: "Not a state pattern — signals that bloc_test library is in use, which implies the file follows bloc-testing conventions. Verifier emits as info, not as state coverage."
```

## setState (`state_management: setState`)

No library — vanilla Flutter mutable widget state. Patterns rely on convention (variable names like `_isLoading`, `_error`), so confidence is lower.

```yaml
setState:
  - name: is_loading_boolean
    pattern: '(bool\s+_?isLoading|_?isLoading\s*=\s*(true|false)|setState\s*\(\s*\(\s*\)\s*=>\s*_?isLoading)'
    covers: [loading]
    confidence: MEDIUM
    note: "Depends on naming convention. Teams use `_isLoading`, `_loading`, `_busy` — verifier should warn if no match found AND state was declared, but not block."

  - name: ternary_on_loading
    pattern: '_?isLoading\s*\?\s*'
    covers: [loading]
    confidence: MEDIUM
    note: "Common Dart UI idiom: `isLoading ? Spinner() : Content()`. Same naming-convention caveat."

  - name: error_field
    pattern: '(String\?\s+_?error|Object\?\s+_?error|_?error\s*=\s*null|_?error\s*!=\s*null)'
    covers: [error]
    confidence: MEDIUM
    note: "Nullable error field is the common setState pattern. False-negatives possible if team uses different names (`failure`, `errorMessage`)."

  - name: ternary_on_error
    pattern: '_?error\s*!=\s*null\s*\?'
    covers: [error]
    confidence: MEDIUM
    note: "Common idiom: `error != null ? ErrorView() : Content()`."

  - name: empty_check
    pattern: '(\.isEmpty\s*\?|isEmpty\s*\?|length\s*==\s*0)'
    covers: [empty]
    confidence: LOW
    note: "Extremely high false-positive rate — `isEmpty` appears in non-state contexts (collection checks, validation). Surface as advisory only."
```

## Other (`state_management: other`)

When the planner cannot detect a known state-management library, the artifact's `state_management:` is set to `other`. The verifier:

- Skips regex-based state coverage entirely.
- Falls back to the must_haves.truths block for verification.
- Emits an advisory note in VERIFICATION.md: "state coverage by regex skipped — state_management: other. Manual review recommended."

## False-positive / false-negative profile

| Library | False Positives | False Negatives | Recommended Verifier Action |
|---------|-----------------|-----------------|------------------------------|
| Riverpod | LOW (`.when()` distinctive) | MEDIUM (`.maybeWhen()` ambiguous) | HIGH patterns block; MEDIUM/LOW advise |
| flutter_bloc | LOW (sealed-class names distinctive) | LOW-MEDIUM (custom names need aliases) | HIGH patterns block; document custom-name escape hatch |
| setState | HIGH (`isLoading`/`isEmpty` generic Dart) | LOW (patterns hit commonly) | All MEDIUM/LOW → advise only; never block setState state coverage |

## Web verification mechanism

Flutter UI TRDs default `platform: [mobile, web]` — both platforms are required coverage. The black-box verifier is different per platform:

| Platform | `tests.integration` invocation | `tests.maestro` invocation |
|----------|--------------------------------|----------------------------|
| mobile   | `flutter test integration_test/` (or `flutter test <path>` for a single file) | `maestro test .maestro/` against a booted emulator |
| web      | `flutter drive --driver=test_driver/integration_test.dart --target=<tests.integration> -d chrome` (chromedriver must be running on port 4444) | **N/A — Maestro is mobile-only by design.** |

**Why is `tests.maestro` mobile-only?** Maestro on Flutter web has a known open upstream issue: **mobile-dev-inc/maestro#2591** (open since July 2025, unresolved mid-2026). `tapOn` cannot find widgets wrapped in `Semantics(label: ...)` on Flutter web even with `SemanticsBinding.instance.ensureSemantics()` enabled. The web semantics overlay is broken upstream, so Maestro's selectability layer fails.

**The substitution model:** Web verification flows through `flutter drive` invoking the SAME `tests.integration` path that mobile uses. `flutter drive` IS the web-tier black-box verifier. The `test_driver/integration_test.dart` driver scaffold is auto-bootstrapped by TRD 10-04a's bootstrap setup task (a one-liner: `Future<void> main() => integrationDriver();`).

**Schema implications:**
- TRDs do NOT carry a `maestro_web: true` opt-in flag — Maestro is mobile-only BY DESIGN.
- `tests.integration` is a single path; the executor (TRD 10-04b) invokes it via the appropriate driver per platform.
- The verifier (TRD 10-05) state-coverage grep runs against `tests.widget` (platform-agnostic; widget tests already work fine on both Flutter mobile and web targets).

**Related upstream issues** (referenced by the verifier for context, no action required from Phase 1 of this objective):
- flutter/flutter#155323 — `Semantics.identifier` on TextField doesn't work on web.
- flutter/flutter#143848 — `ensureSemantics()` breaks DropdownMenu on web 3.19+.
- flutter/flutter#163576 — `ensureSemantics()` click propagation through overlapping widgets.

**If Maestro upstream resolves #2591:** add a `web_maestro_supported: true` flag here and revisit. Until then: `flutter drive` is the web verifier.

## Per-TRD state aliases (deferred — Phase 2)

When a project uses domain-specific State subclass names (e.g., `LoggedIn`, `Cart_Empty`, `OrderProcessing`), the default catalog's regex won't match. The Phase-2 escape hatch (NOT in scope for objective 10) will allow per-TRD aliases:

```yaml
# Future shape (NOT implemented in objective 10):
must_haves:
  artifacts:
    - path: lib/screens/cart_screen.dart
      states: [empty, loading, populated, checkout]
      state_aliases:
        empty: 'Cart_Empty'           # custom regex override per state
        populated: '(Cart_Populated|HasItems)'
      tests: {...}
```

Phase 1 (this objective): fixed catalog only. The planner emits PLANNING INCONCLUSIVE if a TRD's `state_management:` resolves to `other` and the user has not declared either standard library names or supplied custom aliases (Phase 2). For now, `other` falls back to truths-only verification.

## Source

Patterns synthesized from official documentation:
- pub.dev/documentation/riverpod/latest/riverpod/AsyncValue-class.html
- bloclibrary.dev + pub.dev/packages/bloc_test
- docs.flutter.dev/cookbook/testing (setState idioms)

Confidence labels reflect cross-referencing against 10-RESEARCH.md's false-positive/false-negative analysis.

## Consumers

- `agents/planner.md` (TRD 10-03) — detects `state_management:` from project imports; emits PLANNING INCONCLUSIVE if `other` and no truths fallback declared.
- `agents/verifier.md` (TRD 10-05) — loads this catalog, runs regex per declared state per artifact, marks coverage status per the confidence model above.
- `agents/executor.md` (TRD 10-04b) — does NOT consume this catalog directly. Coverage verification happens at verify-work, not execute. BUT the executor uses the "Web verification mechanism" section above to choose the right invocation per platform.
```

# CRITICAL: Use exact regex patterns from RESEARCH.md (lines 510-602). Do NOT improvise. The verifier (TRD 10-05) will reference patterns BY NAME (`when_all_three`, `bloc_loading_state`, etc.).
# GOTCHA: All regex patterns are wrapped in single quotes in YAML to avoid YAML escape conflicts. Backslashes inside single-quoted YAML strings are literal — the consumer (Node.js verifier) will pass them straight to `new RegExp(...)`.
# PATTERN: Reference doc style — match existing `references/anti-patterns.md` and `references/tdd.md` structure (no frontmatter, just markdown).
  </action>
  <verify>
```bash
# File exists and has expected structure
test -f plugins/devflow/devflow/references/flutter-state-patterns.md && echo "OK"

# All 3 state-management sections present
grep -c "^## Riverpod\|^## flutter_bloc\|^## setState" plugins/devflow/devflow/references/flutter-state-patterns.md  # MUST be 3

# Named regex patterns present (these names are referenced by TRD 10-05)
for pattern in when_all_three when_skipped_loading maybe_when when_data_only \
              bloc_loading_state bloc_loaded_data_state bloc_error_state bloc_initial_state \
              is_loading_boolean error_field empty_check; do
  grep -q "name: $pattern" plugins/devflow/devflow/references/flutter-state-patterns.md \
    || { echo "MISSING pattern: $pattern"; exit 1; }
done
echo "All named patterns present"

# Confidence labels present
grep -c "confidence: HIGH\|confidence: MEDIUM\|confidence: LOW" plugins/devflow/devflow/references/flutter-state-patterns.md  # MUST be >= 15

# Web verification mechanism section documented
grep -q "^## Web verification mechanism" plugins/devflow/devflow/references/flutter-state-patterns.md && echo "Web verification section OK"

# Upstream Maestro issue cited as the rationale
grep -q "mobile-dev-inc/maestro#2591" plugins/devflow/devflow/references/flutter-state-patterns.md && echo "Maestro upstream issue cited"

# flutter drive substitution model present
grep -q "flutter drive --driver" plugins/devflow/devflow/references/flutter-state-patterns.md && echo "flutter drive invocation documented"

# No maestro_web opt-in flag — Maestro is mobile-only by design
! grep -q "maestro_web: true\|maestro_web=true\|maestro_web flag" plugins/devflow/devflow/references/flutter-state-patterns.md && echo "OK: no maestro_web opt-in flag"

# Total length sanity (~280 lines target)
LINES=$(wc -l < plugins/devflow/devflow/references/flutter-state-patterns.md)
[ "$LINES" -ge 200 ] && [ "$LINES" -le 450 ] && echo "Length OK ($LINES lines)" \
  || echo "Length out of range: $LINES (expected 200-450)"
```
All checks should pass.
  </verify>
  <recovery>
If a named pattern is missing, re-open RESEARCH.md lines 510-602 and verify the YAML block was transcribed verbatim. The verifier in TRD 10-05 will reference these names by string — typos here will silently break that wiring.

If the file is too short (< 200 lines), some sections were dropped — re-add per the structure above (the Web verification mechanism section is the largest new addition vs. baseline reference docs).

If the maestro_web negative check fails, the prior planning's opt-in language leaked through. Replace with the substitution-model explanation.
  </recovery>
  <done>
- `plugins/devflow/devflow/references/flutter-state-patterns.md` exists.
- All 11 named regex patterns from RESEARCH.md are documented under correct library sections.
- Confidence labels (HIGH/MEDIUM/LOW) appear on every pattern.
- False-positive/false-negative profile table exists.
- A dedicated "Web verification mechanism" section documents the `flutter drive` substitution model (per-platform invocation table + rationale + upstream issue #2591 link).
- No `maestro_web` opt-in flag — Maestro documented as mobile-only-by-design.
- Deferred Phase-2 state-aliases section documents the escape hatch.
- File is 200-450 lines.
- Atomic commit: `docs(10-02): add Flutter state-pattern catalog reference`.
  </done>
</task>

</tasks>

<validation_gates>
<test>npm test</test>
</validation_gates>

<verification>
- [ ] File exists at `plugins/devflow/devflow/references/flutter-state-patterns.md`
- [ ] All 11 named patterns from RESEARCH.md catalog are present (`when_all_three` through `empty_check`)
- [ ] Three library sections present: `## Riverpod`, `## flutter_bloc`, `## setState`
- [ ] False-positive/false-negative profile table present
- [ ] "Web verification mechanism" section documents the `flutter drive` substitution model
- [ ] Upstream issue `mobile-dev-inc/maestro#2591` cited as the rationale for Maestro mobile-only-by-design
- [ ] No `maestro_web: true` opt-in flag mentioned anywhere
- [ ] Per-TRD state aliases section present (deferred Phase 2)
- [ ] Length 200-450 lines
- [ ] No code changes (this is a docs-only TRD)
</verification>

<success_criteria>
- New reference doc shipped at the canonical location consumed by TRDs 10-03 (planner) and 10-05 (verifier).
- All regex patterns are JavaScript-compatible (no PCRE-only constructs).
- Confidence model documented explicitly so the verifier in TRD 10-05 can implement blocker-vs-advisory logic.
- "Web verification mechanism" section explains the per-platform substitution model: mobile uses `flutter test`/Maestro, web uses `flutter drive` invoking the same `tests.integration` path. `tests.maestro` is mobile-only-by-design due to mobile-dev-inc/maestro#2591.
- No `maestro_web` opt-in flag — Maestro mobile-only.
- Atomic commit `docs(10-02): add Flutter state-pattern catalog reference`.
</success_criteria>

<output>
After completion, create `.planning/objectives/10-flutter-ui-verification-process/10-02-SUMMARY.md`
</output>
</content>
</invoke>