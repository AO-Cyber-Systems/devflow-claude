---
objective: 32-visual-eval-default-path-tells-the-truth
trd: "04"
type: tdd
wave: 4
depends_on: ["32-03"]
files_modified:
  - plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs
  - plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs
autonomous: true
severable: true
fixture_strategy: generators
requirements:
  - "UIEVAL-32-10: a run whose verdict is fail exits non-zero"
must_haves:
  truths:
    - "`verify flutter-ui-eval` over a manifest that scores `verdict: fail` exits NON-ZERO, so a CI step can gate on it."
    - "A run scoring `pass` or `pass-with-reviews` exits 0."
    - "The dogfood tests that drive the fail-scoring fixture manifest were updated DELIBERATELY to expect the non-zero exit — not weakened, and the reasoning is recorded."
    - "verifier.md Step 8c continues to work: it captures stdout via `$(...)` and parses JSON; a non-zero exit does not abort the verifier or turn a finding into a crash."
    - "The full `bin/lib` suite is green apart from the three known pre-existing failures (`scanPeer S1`, `check-todos E2E1`, `E2E3 skill dir`), which are NOT attributed to or fixed by this work."
  artifacts:
    - path: plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs
      provides: "process exit code derived from the run verdict"
    - path: plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs
      provides: "Case X1/X2 (exit-code contract) + updated execSync call sites that tolerate the non-zero exit while still parsing the rollup"
  wiring:
    - "run verdict -> process exit code -> CI step pass/fail."
  key_links:
    - "exit code -> CI: without it, a CI step running this command is green regardless of what it found. This is the difference between a gate and a report."
---

# TRD 32-04: A failing run exits non-zero (severable)

<objective>
**Discovered during planning; NOT in AO-Cyber-Systems/aodex#485.** Verified against the
current tree:

    bash -c 'node df-tools.cjs verify flutter-ui-eval __fixtures__/flutter-ui-eval/manifest.json --raw > /dev/null; echo "EXIT=$?"'
    EXIT=0

The checked-in fixture manifest scores `verdict: "fail"` and the process exits 0. **A CI
step running this command is green regardless of what it found.** Everything TRDs 32-01
through 32-03 fix makes the JSON honest; this makes the process honest, which is what a CI
gate actually reads.

**This TRD is severable.** It is the one change in the objective that alters the CLI's
process contract and forces deliberate updates to four existing dogfood tests. If the user
prefers to keep the exit-code contract unchanged, drop this TRD — 32-01 through 32-03 stand
on their own and fully discharge #485.

Resolved intent: kind=plugin, work=bugfix, tdd=strict, depth=comprehensive.
Constraints honored: no_llm_test_data, no_property_based_default, no_gherkin_layer.
</objective>

<context>
- **Depends on 32-03** (same file). Do not start until 32-03 is committed.
- Branch: `fix/ui-eval-default-honesty`, stacked on `feat/ui-visual-eval`.
- **This is the TRD where existing tests legitimately change.** Four dogfood cases (D2, DF1,
  DF2, DF3) call `runJSON('verify flutter-ui-eval <fixture manifest>')`, whose `execSync`
  THROWS on a non-zero exit. The fixture manifest scores `fail` by design (it contains a
  deliberately-broken state). Those call sites must be updated to capture stdout while
  tolerating the non-zero exit. **That is a deliberate change, not a weakening** — record
  the reasoning in the SUMMARY.
- Sibling precedent: `flutter-state-coverage.cjs` also sets no exit code. Note in the
  SUMMARY whether aligning it is worth a follow-up; do NOT change it here.
</context>

<test_list>
- **X1** — a manifest scoring `verdict: fail` (the checked-in fixture manifest) exits
  NON-ZERO, and its stdout is still valid parseable rollup JSON.
- **X2** — a manifest scoring `pass` (a hermetic temp manifest with only labelled,
  not-broken states) exits 0.
- **X3** — a manifest scoring `pass-with-reviews` exits 0 (a review is not a failure).
- **X4** — the `{ error: 'manifest/captureResults not found' }` path's exit code is stated
  explicitly by a test, whatever it is, so verifier.md Step 8c's "record SKIPPED, NEVER a
  hard fail" contract is pinned rather than assumed.

**Regression guard (must stay green):**
- **R4** — after the call-site updates, every pre-existing dogfood case still asserts the
  same rollup CONTENT it asserted before (D2's `network:false`, DF1's `pass`, DF2's
  `overflow/high` + `fail`, DF3's `fails: ['broken-overflow']` + counts). Only the exit-code
  handling around the call changes; not one content assertion is relaxed.
</test_list>

<embedded_context>

<codebase_examples>
The dogfood helper that will throw once the exit code changes
(`flutter-ui-eval-dogfood.test.cjs:21-26`):

```js
function runRaw(argStr, opts = {}) {
  return execSync(`node ${DF_TOOLS} ${argStr}`, { encoding: 'utf-8', ...opts });
}
function runJSON(argStr) {
  return JSON.parse(runRaw(`${argStr} --raw`));
}
```

`execSync` throws on non-zero exit; the thrown error carries `.stdout` and `.status`. The
minimal deliberate update is a helper that returns `{ stdout, status }` and lets each case
assert both — content assertions untouched.

Planning confirmed there is currently no `process.exitCode` or `process.exit` anywhere in
`flutter-ui-eval.cjs` (nor in `flutter-state-coverage.cjs`).

verifier.md Step 8c captures stdout without `set -e`:
```bash
UI_EVAL=$(node ~/.claude/devflow/bin/df-tools.cjs verify flutter-ui-eval "$OBJECTIVE" --raw)
```
so a non-zero exit yields an empty-or-populated `$UI_EVAL` and does not abort the agent.
</codebase_examples>

<gotchas>
- **Use `process.exitCode = 1`, not `process.exit(1)`.** `process.exit` can truncate
  buffered stdout, and X1 requires the rollup JSON to still be fully readable on a failing
  run. Setting `exitCode` lets Node flush and exit naturally.
- Only `verdict: 'fail'` should exit non-zero. `pass-with-reviews` must exit 0 — a review
  is a request for a human look, not a regression.
- The command is dispatched from `df-tools.cjs`; confirm nothing downstream resets
  `process.exitCode` before the process ends.
- `${PIPESTATUS[0]}` inside `bash -c '...'`; never bare `$?` after a pipe. When asserting
  exit codes in a `verify` block, run the command WITHOUT a pipe and read `$?` directly, or
  wrap explicitly.
</gotchas>

<anti_patterns>
- Do NOT relax any existing content assertion to make the call sites compile. If DF3
  asserted `fails: ['broken-overflow']` before, it asserts it after. Only exit-code handling
  moves.
- Do NOT make the error/not-found path exit non-zero without deciding deliberately — Step 8c
  routes it to `SKIPPED, NEVER a hard fail`. X4 exists to pin whatever you decide.
- Do NOT change `flutter-state-coverage.cjs` here. Out of scope; note it for follow-up.
- No new dependencies.
</anti_patterns>

<error_recovery>
- If the whole dogfood file starts erroring with `Command failed`, that is `execSync`
  throwing, i.e. the expected consequence — update the helper, do not revert the exit code.
- If X1's stdout comes back empty, you used `process.exit()` instead of `process.exitCode`.
</error_recovery>

</embedded_context>

<tasks>

<task type="auto" tdd="true">
  <name>RED: the exit-code contract</name>
  <files>plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs</files>
  <action>
Write X1 first (one test at a time), then X2-X4.

X1: run `node df-tools.cjs verify flutter-ui-eval <fixture manifest> --raw` via a helper
that captures BOTH stdout and the exit status without throwing (catch `execSync`'s error and
read `.stdout` / `.status`, or use `spawnSync`). Assert:
  - the exit status is non-zero — message: "a run scoring `fail` must exit non-zero or no CI
    step can gate on it"
  - `JSON.parse(stdout).verdict === 'fail'` — the rollup is still fully readable

Run it and RECORD the RED output verbatim. Recorded pre-fix behaviour from planning:
`EXIT=0` on the fixture manifest whose verdict is `fail`.

COMMIT THE RED TESTS NOW before implementing (maxTurns ceiling):

    node ~/.claude/devflow/bin/df-tools.cjs commit "test(ui-eval): RED — a failing run must exit non-zero" --files plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs
  </action>
  <verify>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval-dogfood.test.cjs 2>&1 | tail -30; exit ${PIPESTATUS[0]}'
```
exits NON-ZERO with X1 named in the failure. `git log -1 --stat` shows the RED tests committed.
  </verify>
  <done>
X1-X4 exist, are committed, and X1 has been OBSERVED to fail with its output recorded.
`flutter-ui-eval.cjs` is unmodified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>GREEN: derive the exit code from the verdict, and update the four call sites deliberately</name>
  <files>plugins/devflow/devflow/bin/lib/flutter-ui-eval.cjs, plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs</files>
  <action>
1. In `cmdVerifyFlutterUIEval`, after `output({...})`, set
   `process.exitCode = rollup.verdict === 'fail' ? 1 : 0` (only `fail` is non-zero).
   # CRITICAL: `process.exitCode`, NOT `process.exit()` — X1 requires the full rollup JSON
   #   to still reach stdout on a failing run.
2. Decide and implement the not-found/invalid-manifest exit code, pinned by X4. Recommended:
   keep it 0 so verifier.md Step 8c's "record SKIPPED, NEVER a hard fail" contract holds
   unchanged. State the decision in the SUMMARY either way.
3. Update the four existing dogfood call sites (D2, DF1, DF2, DF3) to use the
   non-throwing helper. **Every content assertion stays exactly as it was** — this is a
   call-site change, not an assertion change. Diff-review your own change before committing:
   `git diff plugins/devflow/devflow/bin/lib/flutter-ui-eval-dogfood.test.cjs` should show
   no removed `assert` on rollup content.

Commit as `test:` -> `feat:` pairs. Record in the SUMMARY, explicitly: which four tests
changed, that only their invocation changed, and why (the fixture manifest scores `fail` by
design, so `execSync`'s throw-on-non-zero is now the expected path).
  </action>
  <verify>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test flutter-ui-eval.test.cjs flutter-ui-eval-dogfood.test.cjs flutter-ui-eval-planner-default.test.cjs flutter-ui-eval-bootstrap.test.cjs 2>&1 | tail -12; exit ${PIPESTATUS[0]}'
```
exits 0. And the direct exit-code probe (no pipe, so `$?` is unambiguous):
```
bash -c 'node plugins/devflow/devflow/bin/df-tools.cjs verify flutter-ui-eval plugins/devflow/devflow/bin/lib/__fixtures__/flutter-ui-eval/manifest.json --raw > /dev/null; echo "FAIL_EXIT=$?"'
```
prints `FAIL_EXIT=1`.
  </verify>
  <done>
A failing run exits 1 with its rollup intact. A passing or pass-with-reviews run exits 0.
No rollup content assertion was relaxed.
  </done>
</task>

<task type="auto">
  <name>Objective-level regression: full bin/lib suite with the known failures named</name>
  <files>plugins/devflow/devflow/bin/lib/</files>
  <action>
Run the full `bin/lib` suite and record the result as the objective's regression evidence.

    bash -c 'cd plugins/devflow/devflow/bin/lib && node --test 2>&1 | tail -20; exit ${PIPESTATUS[0]}'

**Known pre-existing failures — do NOT attribute to this work and do NOT fix here:**
`scanPeer S1`, `check-todos E2E1`, `E2E3 skill dir`. They fail identically on clean
`origin/main` (verified in a throwaway worktree). Main runs 2,164 tests; the
`feat/ui-visual-eval` branch runs 2,191.

In the SUMMARY record: total tests, pass, fail, and the NAME of every failure. If a failure
appears that is NOT one of the three known ones, it belongs to this objective — diagnose it,
do not explain it away. Report the ui-eval test delta (baseline was 60 before this
objective) explicitly.

# GOTCHA: `trd-summary-count-underreports-built-work` — count the tests actually added
#   across all four TRDs, not just the ones this TRD touched.
  </action>
  <verify>
The SUMMARY contains the full-suite tail output verbatim, with every failure named and each
one matched against the three known pre-existing failures. Exit-code evidence included.
  </verify>
  <done>
Full-suite regression recorded. Any failure beyond the three known ones is diagnosed, not
attributed away.
  </done>
</task>

</tasks>

<verification>
```
bash -c 'cd plugins/devflow/devflow/bin/lib && node --test 2>&1 | tail -20; exit ${PIPESTATUS[0]}'
```
Expected: the three known pre-existing failures only. Plus the direct exit-code probe above
printing `FAIL_EXIT=1`.
</verification>

<success_criteria>
- SC-6 (objective) — a `fail` verdict exits non-zero.
- SC-8 (objective) — suite green apart from the three named pre-existing failures.
</success_criteria>

<output>
On completion write `32-04-SUMMARY.md` containing:
- The recorded RED output for X1 (`EXIT=0` on a `fail` verdict).
- The four changed dogfood call sites, with the explicit statement that only invocation
  changed and no content assertion was relaxed.
- The not-found-path exit-code decision.
- The full-suite tail output with every failure named.
- A note on whether `flutter-state-coverage.cjs` should get the same treatment (follow-up,
  not done here).
</output>
