rails: cd rails && bin/rails server -p 3100
tunnel: cloudflared tunnel --url http://localhost:3100 run devflow-relay 2>/dev/null || echo "Cloudflare Tunnel not configured. Run: cloudflared tunnel create devflow-relay"
